# Signal Desk — MVP 기능 백로그

1단계 스캐폴딩 이후 붙일 핵심 기능 리스트. Signal APT의
["전체 기능 백로그"](https://github.com/c-yeonwoo/apt-signal) 문서 형식(우선순위·범위·리스크)을
따르되, 사용자가 정한 **phase1(비로그인 데이터/엔진 레이어)**·**phase2(로그인 사용자 기능)** 축을
그대로 유지한다. 우선순위: **P0** 없으면 서비스 정체성이 안 섬 · **P1** 핵심 차별화 · **P2** 확장.
범위: S(일) · M(수일) · L(1~2주) · XL(2주+).

## 0. 데이터 소스 결정 (P0, 선행 과제)

한국 주식부터 시작(요청사항: "정보를 얻기 쉬운 한쪽에서 시작"). 조사 결과 아래 조합이 **무료·공식**으로
기술적분석+기본적분석+백테스트+모의 자동매매를 전부 커버한다 — 별도 유료 API 없이 phase1 전체가
이 스택만으로 가능:

| 소스 | 용도 | 비고 |
|---|---|---|
| [KIS Developers(한국투자증권 오픈API)](https://apiportal.koreainvestment.com/intro) | 실시간 시세(WebSocket)·주문·**모의투자 계좌** | ✅ 앱키·모의계좌(1억원) 발급·연동·실키 검증 완료(2026-07-02). **모의투자 리그**로 실제 돈 없이 자동매매봇 검증 가능 |
| [KRX Open API](https://openapi.krx.co.kr/) | 종목기본정보·일별매매정보(시가총액) | ✅ 서비스 이용신청 승인·실응답 검증 완료(2026-07-02). 시가총액 상위 200(보통주만)으로 코스피200 근사 |
| DART 공시 API | 재무제표(ROE/부채비율/매출성장 산출 원천) | ✅ 키 발급·연동·실응답 검증 완료(2026-07-02). PER/PBR은 KRX 종가 결합 필요해 후속 작업 |
| [pykrx](https://github.com/sharebook-kr/pykrx) | 위 API 공백 시 스크래핑 폴백 | 비공식, 최후 수단으로만 |

미국주식은 위 스택이 자리잡은 뒤(phase1 후반~phase2) AlphaVantage 등으로 확장 — 지금은 범위에서 제외.

**진행 상황(2026-07-02)**:
1. `pykrx`의 시장 전체/지수구성종목 계열 엔드포인트(`get_index_portfolio_deposit_file`,
   `get_market_cap_by_ticker`, `get_market_ohlcv_by_ticker`, `get_market_ticker_list`)가 전부
   KRX 응답 스키마 변경으로 깨져 있음을 직접 재현 확인(JSON 파싱 에러). 종목별 시계열
   (`get_market_ohlcv_by_date`)과 단일 종목명 조회는 정상 — 계속 사용.
2. KRX_API_KEY 발급받아 공식 Open API를 직접 호출해봄 — **키 발급과 별개로 서비스마다
   openapi.krx.co.kr에서 개별 "API 이용신청" 승인이 필요**함을 확인(전 엔드포인트 401
   Unauthorized). 또한 **공식 Open API 카탈로그에 "지수 구성종목"(코스피200 편입종목) 서비스가
   아예 없음**도 확인 — 지수 시세(숫자)만 제공, 종목 리스트는 미제공.
3. → 사용자 결정: **"유가증권 종목기본정보"+"유가증권 일별매매정보" 서비스 신청 후 시가총액
   상위 200종목으로 코스피200 근사**. `ingest/krx_open_api.py` 구현, `ingest/krx.py:universe()`가
   이 경로를 우선 시도하고 실패 시 대형주 30종목 폴백으로 자동 전환(실제 미승인 키로 폴백 동작
   확인 후, 승인 완료되어 실응답으로 재검증까지 완료).
4. **서비스 승인 완료·실응답 검증(2026-07-02)**: 필드명 추정(`ISU_CD`/`ISU_NM`/`MKTCAP`)이 실제
   응답과 정확히 일치. 다만 응답에 **우선주가 섞여 나오는 것을 발견**(예: `005935 삼성전자우`가
   시가총액 기준 최상위권에 노출) — 코스피200은 보통주만 편입하므로 `_is_common_share()`(종목코드
   마지막 자리 0=보통주, 5~9=우선주 관례, 실데이터로 확인)로 걸러내도록 수정. 현재 실제
   200종목(보통주만) 유니버스로 `sigdesk fetch` 완료, 백테스트 표본도 BUY 46건→917건으로
   확대되어 통계적으로 훨씬 유의미해짐(승률 50.8%, avg_ret_5d +1.54%, avg_ret_20d +7.53%).
5. **PER/PBR 완성**: DART 재무제표만으론 못 구했던 PER/PBR을 KRX `MKTCAP`(이미 유니버스 랭킹에
   쓰던 값)과 결합해 계산 — `PER = 시가총액/당기순이익`, `PBR = 시가총액/자본총계`(주당지표로
   나눴다 곱하는 것과 수학적으로 동일, 발행주식수 없이 계산 가능해 더 안정적). 부가로 DART
   응답에 연결(CFS)·별도(OFS) 재무제표가 같은 계정명으로 섞여 오는 것을 발견해(삼성전자 기준
   자본총계 CFS 436.3조 vs OFS 254.3조로 값 자체가 다름) CFS 우선 사용하도록 수정 — 이전엔
   응답 순서에 우연히 의존하고 있었음. 199/200종목 재무데이터 중 177종목 PER, 199종목 PBR 산출
   완료(적자 기업은 PER 계산 제외 — 업계 관례). 삼성전자 시가총액을 실제 발행주식수×현재가로
   교차검증해 계산 파이프라인 정확성 확인.
6. **KIS 모의투자 연동 시작**: 모의계좌(1억원) 개설 + 앱키 발급 완료. `broker/kis.py` 구현 —
   인증(oauth2/tokenP)·잔고조회·주문(현금) 실키로 검증. **토큰 발급 API에 엄격한 rate limit
   실제 확인**(짧은 간격 재요청 시 HTTP 403) — 반드시 파일 캐시(`data/cache/kis_token.json`,
   1일 유효)로 재사용해야 하며 매 호출마다 새로 발급하면 안 됨. 잔고조회 파라미터 중 `OFL_YN`
   (빈 문자열이어도 키 자체는 필수)이 빠지면 `rt_cd=2` 에러가 나는 것도 실응답으로 확인.
   실제 잔고 1억원(현금)·보유종목 0건 정상 조회 확인. 남은 것: 시그널→주문 자동 실행 루프,
   포지션 추적(체결 후 평단가·보유량 갱신), 리스크 엔진(#8)과 연결한 자동 청산.

## Phase 1 — 비로그인 (데이터·엔진 레이어)

시그널 엔진 자체가 서비스 정체성이므로 이 구간이 최우선. `brightdesk`에서 이미 검증한 3팩터
가중 로직(기술 0.35·기본 0.30·KB 0.35, [NOTES-cherrypick.md](NOTES-cherrypick.md) 참고)을
그대로 재사용하되, KB(감성) 팩터는 데이터 소스가 갖춰지는 후순위로 미루고 기술+기본 2팩터로 시작.

| # | 기능 | 가치 | 우선 | 범위 | 의존/리스크 | 상태 |
|---|---|---|---|---|---|---|
| 1 | 기술적분석 엔진(RSI·MACD·MA) | 시그널 트리거의 절반 축 | P0 | M | KIS/KRX 시세 캐시 선행 | ✅ `signals/indicators.py` |
| 2 | 기본적분석 엔진(PER·PBR·ROE·매출성장·부채비율 스코어) | 나머지 절반 축 — "감이 아니라 성적표" | P0 | M | DART 공시 파싱 | ✅ 전항목 완성 — DART(ROE·부채비율·매출성장) + DART×KRX 결합(PER·PBR), 실키 검증 완료 |
| 3 | 통합 시그널(기술+기본+저평가+낙폭과대 가중합) | 서비스 핵심 산출물 — "종합 분석" 요청(2026-07-02)으로 2팩터→4팩터 확장 | P0 | M | #1·#2·#13 | ✅ `signals/engine.py`(가중치 기술.35/기본.30/저평가.15/낙폭과대.20, 계산 불가·무해당 팩터는 가중치 0으로 재정규화), `signals/reversion.py`(낙폭과대 반등/단기과열 조정 — 순수 가격 데이터), `/api/signals`. 정성적·시국·정세·산업사이클·정치 등 LLM·뉴스데이터 필요 팩터는 #11·#17·아래 "아이디어" 참고 — 이번 범위 밖(사용자가 "LLM 없이 되는 팔터부터"로 명시 확정) |
| 4 | 백테스트 성적표(적중률·5일/20일 수익률) | Signal APT와 동일한 "신뢰 해자" — 유료 전제 조건 | P0 | L | #3 + 과거 시세(KRX) | ✅ `method: price_based_v2`(기술+낙폭과대 — 둘 다 순수 가격이라 과거 시점 재현 가능), `/api/backtest` — 저평가/기본은 시점별 재무 스냅샷 없어 과거 재현 범위 밖(TODO) |
| 5 | 시장 국면(강세·과열·조정·약세) + 거시 시황 | 시그널에 국면·거시 오버레이 — 개별 탭 아님(APT 패턴 유지) | P1 | M | 지수·거래대금 임계값 설계 | ✅ `signals/regime.py`(국내 breadth+모멘텀 근사)+`ingest/fred.py`·`signals/macro.py`(미 CPI·기준금리·10년물·나스닥·VIX → 우호/비우호 판정, 한국 동조 전제). `/api/regime`·`/api/macro`, 시그널 탭 상단 "시황 스트립"에 국면 pill + 거시 판정 + 지표칩. 정식 지수 기반 판정은 후속 |
| 6 | 최근 이슈 흐름(이슈 → path/alt → 파급 → 관심종목, 수동 Sonnet) | 뉴스 파급 갈래로 시황 읽기 — 가설 검증 아님 · 시그널/봇과 독립 | P1 | L | ⚠️ 관심%·갈래%(룰) · 종목은 밸류체인. 문장은 **관리자 수동 Sonnet**. 설계: [docs/scenario-hypothesis.md](docs/scenario-hypothesis.md) | ✅ `hypo:v4` · 이어읽기 라벨 · action 종목 · ECharts · refresh=admin |
| 7 | 실시간 자동투자봇(시그널 기반 모의투자 포트폴리오) | Track A(자동 운용) 데모의 핵심 — brightdesk에서 이미 시도했던 개념을 KIS 모의투자로 실화 | P0~P1 | XL | KIS 모의투자 계좌 연동 + 리스크 엔진(#8) 선행 필요 | ✅ `bot.py` 오케스트레이터(리스크→시그널 청산 우선순위, 동일가중 포지션 사이징, KIS 잔고 재대사) + FastAPI lifespan 백그라운드 루프(`BOT_RUN_INTERVAL_MINUTES`, 기본 10분, 기본 OFF) + 포트폴리오 탭 UI. 실서버로 KIS 연결·잔고 조회 확인, 실주문은 아직 미실행(사용자 확인 후 진행 예정) |
| 8 | 리스크 엔진(stop-loss/take-profit/trailing) | 자동매매봇 없이는 위험한 뼈대 — brightdesk 룰(−7%/+15%/−5%) 재검증 후 채택 | P0 | M | #7과 사실상 세트 | ✅ `signals/risk.py` — 포지션 모델 없어서 순수 함수로만(평단가·현재가·고점 입력) 구현, #7 붙을 때 바로 사용 |

**순서 권장**: 0(데이터소스) → 1·2(병렬 가능) → 3 → 4(성적표) → 8(리스크 룰) → 7(자동매매봇) → 5·6.
자동매매봇(#7)은 성적표(#4)로 신뢰를 먼저 증명한 시그널에만 태워야 함 — 순서를 앞당기지 않는다.

## Phase 2 — 로그인 사용자 기능

| # | 기능 | 가치 | 우선 | 범위 | 의존/리스크 | 상태 |
|---|---|---|---|---|---|---|
| 9 | 포트폴리오 시나리오 분석(공격적·균형·보수적 전략별 N년 후 예상 수익) | Signal APT "결론" 탭과 동급 킬러 기능 — 내 돈으로 결론까지 | P1 | L | 몬테카를로 또는 과거 리샘플링, phase1 시그널·리스크 룰 필요 | ✅ `signals/scenario.py`(부트스트랩 MC, 성향=주식노출도, 전략별 terminal p10~p90·CAGR·연도별 fan, 고정시드) + `/api/scenario` + 내 포트폴리오 탭. 시세가 스케일 데이터라 절대금액은 예시(면책) — 전략 상대비교 용도 |
| 10 | 산업 분류(~10개) + 밸류체인 맵 | 관심 산업군 스터디 — 트리맵(#12)의 분류 기준이 되는 선행 데이터 | P1 | M~L | 분류 체계 확정 필요(GICS 준용 vs 자체 10분류) | |
| 11 | 종목별 KB 수집(뉴스·증권사 리포트·개인분석 크롤링) | #15 해설 v2의 원료 — brightdesk source_registry/kb_facts 패턴 재사용 | P1 | M~L | 네이버뉴스/RSS, brightdesk에 이미 연동 이력. 소스별 reliability 값도 [NOTES-cherrypick.md](NOTES-cherrypick.md) 참고 | ⚠️ **Decision 아키텍처** [docs/kb-decision-architecture.md](docs/kb-decision-architecture.md) — P0~P3(shadow) 완료. 정성 `combine` 미투입 유지 · 다음: priority/threshold(게이트+승인) · 자동 조사큐 |
| 12 | 관심종목/포트폴리오 섹터 히트맵(트리맵) | APT 지도의 대체재(번역표 §4) | P2 | M | #10 산업분류 선행 | ✅ `/api/portfolio/heatmap` + 내 포트폴리오 탭 ECharts treemap(섹터→종목, 크기=평가액·색=손익률). 국내 `reference/sectors`+해외 GICS 혼합 |
| 13 | 밸류에이션(저평가) 스크리닝 | 이미 5탭에 자리 있음 — #2 스코어 기반으로 실데이터 연동 | P1 | M | #2 | ✅ `signals/valuation.py`(PER/PBR 유니버스 내 상대 percentile), `/api/valuation`, 저평가 탭 실데이터 렌더링. 섹터 분류(#10) 붙기 전까지는 전체 유니버스 기준 근사 |
| 14 | 통합 후보 뷰 + 기회도(눌림목·낙폭과대·IPO·실적서프라이즈·턴어라운드) | 여러 후보 유형을 기회도순 한 리스트로 | P1~P2 | L | #3 시그널 + #13 밸류에이션 | ⚠️ #3의 "낙폭과대" 팩터(`signals/reversion.py`, 종합 시그널 점수 컴포넌트)와 이름은 같지만 다른 개념 — 여긴 후보 유형 분류·리스트업이 목적. 계산 로직 재사용은 가능(같은 N일 누적수익률+RSI 조합) · ✅ `signals/opportunity.classify`(팩터→유형 태그: 낙폭과대·저평가·모멘텀·실적우량·정성호재) + 시그널 리스트 유형 뱃지·필터 칩. IPO·실적서프라이즈·턴어라운드는 데이터 필요해 제외 |
| 15 | AI 리포트(관심 80%·신규 20% 블렌드) | 이미 스텁 존재 — ANTHROPIC_API_KEY 연동만 남음 | P2 | M | #11 뉴스로 맥락 보강 | |
| 17 | **시그널 해설 v2(LLM 기반, `narrative.py` 교체)** | 지금 규칙기반 해설(`signals/narrative.py`)을 종목별 KB(#11)로 근거를 보강한 LLM 해설로 승격 | P2 | M | #11 + ANTHROPIC_API_KEY. 종목당 매 요청 LLM 호출은 비용·지연 크므로 **결과를 캐시**(예: `kv` 테이블에 종목+데이터스냅샷 해시로 키). 규칙기반 v1은 폴백으로 유지 | ✅ `narrative.explain_llm`(근거+KB만 근거, 환각·투자권유 금지) + `/api/narrative`(스냅샷 해시로 kv 캐시) + 상세뷰 비동기 '🤖 AI 해설' 덮어쓰기. LLM 미설정/실패 시 v1 폴백 |
| 16 | 알림/리텐션(가격·시그널 변동 시 알림) | "매달 다시 열 이유" — Signal APT 백로그에서도 P1 취급 | P2 | M | #3 시그널 변동 감지 | ✅ 인앱 알림 — 관심종목 시그널 kind 변동 감지(`_scan_alerts`, 조회 시점 비교, 최초관측 기록만) + 헤더 🔔 벨·안읽음 뱃지·목록. 가격 급등락·외부채널(이메일/푸시)은 후속 |

## 탭 배치 메모

**PO 결정(2026-07-02, #7 자동매매봇 착수 시점)**: "관심·비교" 탭을 **"포트폴리오"** 탭으로 개편,
내부에 "자동매매"(기본)·"관심종목" 두 세그먼트로 분리. 탭 개수는 5개 그대로 유지(APT 나침반
규칙 §6 "탭 무한증식 금지"). brightdesk 원래 구조(Track A 자동 운용 = `/portfolio`)와도 일치.

현재 5탭(시그널·저평가·후보·**포트폴리오**·AI리포트) 기준:
- #5(국면)·#6(전망)은 **시그널 탭 안 오버레이**로(새 탭 아님).
- #9(시나리오)는 **포트폴리오 탭 자동매매 세그먼트 확장**으로 검토(#7과 같은 자리).
- #10·#12(산업/히트맵)는 **저평가 탭 세그먼트**로 우선 검토.
- #11(뉴스)은 원래 Signal APT DESIGN.md 나침반엔 독립 탭이었으나, 우리는 시그널/후보 탭 내 패널로
  먼저 시도 — 콘텐츠 양이 많아지면 그때 탭 승격 검토.

## 규제/표현 유의 (Signal APT 백로그에서 이미 지적된 리스크, 주식도 동일 적용)

- #6(미래 전망)·#7(자동매매봇)·#9(시나리오 수익 예상)은 "확정 수익 보장"으로 읽히지 않게 확률·범위
  표현 강제, 면책 문구 상시 노출. 유사투자자문업 등록 요건 여부는 별도 확인 필요(법률 자문 대상,
  이 리포에서 결론 내지 않음).

## 아이디어 (일부 착수)

- **우리만의 고유 시그널 공식**: 2026-07-02 사용자 요청("종합 분석 = 기술+기본+저평가+낙폭과대/단기과열
  +정성적+시국+정세+경제지표+산업사이클+밸류체인+테마+정치+백테스트+미래전망을 신뢰도 가중 조합")에
  따라 1단계로 **LLM·신규 데이터 없이 계산 가능한 4팩터(기술·기본·저평가·낙폭과대)를 #3에 통합
  완료**(가중치 재정규화 방식 유지, brightdesk 원 공식에서 확장). 사용자가 명시적으로 "LLM 없이
  되는 팔터부터"를 선택해 이번 범위로 확정.
  남은 팩터(정성적/시국/정세/경제지표/산업사이클/밸류체인/테마/정치)는 전부 뉴스·리포트 등 KB
  데이터(#11)나 지수/매크로 데이터(#5·#6 정식판)가 선행돼야 함 — 그 전까지 LLM을 붙이면 근거 없는
  환각 위험. 붙일 때도 사용자가 "정성적 판단을 하나의 팩터 점수로만" 선택했으므로, LLM은 팩터 점수
  하나만 산출하고(예: 가중치 0.15~0.20 내외 검토) 매매 실행·사이징·리스크 판단 권한은 계속
  결정론적 코드(`bot.py`/`risk.py`)가 갖는다 — 자동매매봇을 LLM 에이전트로 만들지 않는다(안전·재현성
  ·감사가능성 우선, PO 판단).
- **자동매매봇의 "수익 극대화" 목표**: 사용자가 명시했지만, 리스크 엔진(#8)의 손절·트레일링 우선순위를
  낮추지 않는 선에서 반영 — 극대화를 이유로 손절 규칙을 완화/생략하지 않는다(계좌 전액 손실 방지가
  우선). 포지션 사이징 고도화(#9 시나리오, 동일가중 이상)가 이 목표에 더 가까운 다음 단계.
- **급변 학습(이벤트 보드)** — 2026-07-25 아이디어, 미착수.  
  |등락|≥10% 종목을 추려 **원인 분석·학습**으로 본다(시그널·매수권 아님).  
  - **포지션**: 「오늘은 시그널이 없어도, 시장이 크게 움직인 종목은 이렇게 읽는다」· 셀프서브·강의 소재·D7  
  - **이미 있음**: `change_pct` · 낙폭과대/단기과열 팩터 · 종목 상세 `recent_moves`(KB→LLM)  
  - **없음**: ±10% 유니버스 보드 + 원인 한곳 모음  
  - **MVP 초안**: 국내 |등락|≥10%(장중 라이브/장후 종가) → 카드(종목·±%·섹터·추정 원인 2~3줄, KB·공시 우선, 없으면 미확정) → 인사이트 학습 옆 또는 시그널 하단「급변 학습」·시그널 pill과 색 분리 → 클릭 시 시그널 상세 딥링크  
  - **비목표**: 봇·문턱·`combine`·기후 q·매수권 연동. LLM 원인 단정·환각 금지.  
  - **주의**: 조정장 후보 폭주 → 상위 N·시총 필터. 이름 후보: 급변 학습 / 이벤트 보드.
- **숏 사이드(하락장 베팅)** — 2026-07-25 논의. **1단계만 착수**, 봇 연동은 보류.
  - **1단계(완료)**: 실측 트래커에 `sell_precision_pct`(매도 찍은 것 중 내린 비율) 추가 →
    신뢰 스트립·관리자 실측 패널 노출. 매수와 표본 가드 분리, 봇·문턱·`combine` 미반영.
  - **1.5단계(완료, 2026-07-25 진단)**: 정밀도 **절대값으로는 판정 불가**임이 드러나 기준선을 붙였다.
    −3% 조정장이면 무작위 매도도 정밀도 60%(σ=12% 가정)인데 UI는 55%를 초록으로 칠하고 있었다 →
    `baseline.up_pct/down_pct`(항상 매수·항상 매도 naive 정밀도) · `buy_lift_pp`/`sell_lift_pp` ·
    `*_ci_pp`(95% 신뢰구간 반폭) 추가, UI 색 임계를 절대값 55/45 → **리프트 ±`MIN_LIFT_PP`(3%p)** 로 교체.
  - **2단계 조건**: 매도 표본 ≥ 20 **AND** `sell_lift_pp ≥ +3%p` **AND** 리프트가 `sell_precision_ci_pp`
    보다 클 때만 **페이퍼 한정** 숏 장부/인버스 ETF 헤지 모드 설계 착수. 정밀도 절대값(예: 60%)은
    조건이 아니다 — 하락장 드리프트를 실력으로 오독한다. 그전에는 설계도 시작하지 않는다.
  - **비목표**: 실주문·개인 직접 공매도 경로. 숏 "권유" 문구(유사투자자문 리스크 롱보다 큼).
  - **주의**: 숏 손익 비대칭(이익 상한 100%·손실 무한) · 숏스퀴즈 · 대차수수료 캐리 ·
    인버스 ETF 변동성 감쇠(레버리지형 특히, 장기보유 부적합). 개인 공매도 제도는 변경이 잦아 별도 확인 필요.
  - **학습 레일**: 「하락장에 숏이 왜 어려운가」를 급변 학습 소재로 — 강의 부대 모델과 정합.
- **아침 브리핑(텔레그램 채널)** — 2026-07-25 완료. `digest.build_morning()` + 봇 루프 훅.
  평일 KST 07시(`MORNING_DIGEST_HOUR`, `off` 가능) 이후 첫 틱에 1회. 유저별이 아니라 **시장 요약**
  이라 채널 공용(보유·계좌 정보 미포함).
  - **내용**: 시장 ZONE · 유효 매수문턱(+상향 사유) · 매수 시그널 상위 · 매수 근접(문턱 0.5 이내) ·
    실측 상태(표본 20 미만이면 숫자 대신 누적중) · 「매수 권유 아님」.
  - **매수 0일에도 발송한다** — 「기준을 넘은 종목이 없습니다. 오늘은 기다리는 날입니다」 +
    문턱이 오른 사유 + 근접 종목. 앱의 '매수 0일 히어로'와 같은 톤(D7 레일).
  - **다음(미착수)**: 원칙 카드가 생기면 브리핑에 「당신 원칙」 한 줄 대입 · 급변 학습 붙으면 ±10% 요약.
- **advisor(봇 LLM 자문) 성적표** — 2026-07-25 착수. `/api/advisor-shadow`(관리자).
  `advisor.select_buys()`는 LLM이 봇 매수 종목에 영향을 주는 **유일한 경로**인데
  「점수순으로 그냥 샀을 때보다 나았나」를 재는 지표가 없었다 → 회차마다 LLM 선택과
  결정론적 폴백 선택을 함께 기록하고 실현수익으로 비교(`signals/advisor_shadow.py`).
  - **채점 규약**: `accuracy`와 동일(다음 거래일 진입 · 20거래일 성숙). 겹친 종목은 상쇄되므로
    **갈린 종목(`llm_only` vs `base_only`)만** advisor의 순효과. 하루 첫 회차만 기록(잔여 슬롯 편향 방지).
  - **판정 규칙 정정(2026-07-25 진단)**: 기존 「갈린 쌍 ≥ 20이면 판정」은 **통계적으로 성립하지 않는다**.
    σ(20거래일 수익 표준편차)=12%면 unpaired 20표본 평균차의 SE가 ±3.8%p → 유의 임계 ±7.4%p,
    검정력 80% 최소검출차 ±10.6%p. Δ=2%p를 검출하려면 한쪽 565건(≈1.4년)이 필요하다.
    → `delta_se_pp`·`delta_ci95_pp`·`delta_significant`를 실측 분산으로 계산하고,
    `verdict_ready = 표본 도달 AND 유의`로 바꿨다. **표본 수만으로 on/off 판단 금지.**
  - **구조 편향 해소(완료, 2026-07-27)**: `base_only`=점수 상위·`llm_only`=그 아래라 unpaired
    delta는 advisor에게 불리했다. → 회차 안 pool 순위로 교체 쌍을 맞춘 **`paired_delta`** 가
    판정 정본. unpaired `delta_pct`는 진단용으로만 남긴다. `record(style=)` + 레퍼런스 uid
    폴백으로 **성향별(`by_style`) 분리 집계**. UI·감사 스냅샷은 paired를 본다.
  - **기권/실패 구분(완료)**: `select_buys`가 `[]`(기권) / `None`(사용 불가)을 구분한다. 이전에는
    `picks or None` + 폴백 때문에 LLM의 「살 게 없다」가 **상위 점수 강제 매수로 번역**됐고,
    프롬프트에도 기권 선택지가 없었다(하락장에 정확히 반대 행동). 프롬프트에 반론 검토·기권 경로 추가.
    기권 회차는 shadow에서 **현금 0% 수익**으로 채점한다(비워두면 delta가 폴백에 유리해진다).
  - **비목표(당시)**: 선별 로직·수량·문턱·리스크 규칙 변경. → **일부 해제(2026-07-27)**:
    paired 유의 패배 시 LLM 선별 **kill**(기본 fallback=기권) + **Challenger veto**(제거만).
    수량·문턱·리스크 규칙은 여전히 미변경. LLM끼리 합의로 on/off 하지 않음.
  - **완료(2026-07-27)**: `advisor.advise` · `advisor_shadow.gate` · `/api/advisor-harness`
    · 실패 사유(`no_key|api_fail|parse_fail|out_of_pool|killed_shadow`).
  - **다음**: advisor는 슬롯 있는 **모든 회차** 호출되는데 shadow는 하루 첫 회차만 채점
    → 비용·측정 불일치.


## 북극성 지표 (2026-07-21 합의)

셀프서브 개인투자자 · 수익화=강의 부대(초대제 가능) · 기후=뱃지 노출만.  
**D7** 정의·보조지표: [docs/north-star-d7.md](docs/north-star-d7.md).

- **계측 도입(2026-07-25)**: 「북극성」이라 부르면서 **코드에 D7이 한 줄도 없었다** — 4일간 만든
  기능이 모두 리텐션 가설인데 검증 수단이 없었다. → `signal_visits` 테이블 + `GET /api/signals`
  훅 + `db.d7_metrics()` + `GET /api/d7`(관리자) + 엔진 탭 「북극성 D7」 카드(주 코호트).
  아침 브리핑에는 `PUBLIC_BASE_URL` 기반 `/#signal` 복귀 링크와 전일 대비 증감을 추가했다
  (링크·변화 없는 브리핑은 읽고 끝나서 D7에 구조적으로 기여하지 못한다).
- **판독 규칙**: 코호트 미완성(가입 7일 미경과)은 분모에서 빼고 `pending_users`로 센다.
  성숙 코호트 0명이면 `d7_pct = null`(**판정 불가**) — 0%로 표시하면 정반대로 읽힌다.
  소급 집계 불가 → **첫 유효 판정은 배포 + 8일 이후.**
- **미착수**: 개선 목표치 합의(예: D7 40%), 브리핑 열람→앱 복귀 전환율(링크에 utm 미부착).

## 매수권 캘리브레이션 — 절대 문턱 → 횡단면 분위 (2026-07-26)

**증상**: 오픈 13일, 페이퍼 수익률 마이너스, 페이퍼봇 거래 정지.

**진단(추측 아님, PIT 기록)**: `signal_history.parquet` 2,000건(200종목 × 10거래일)에서
매수 시그널 **1건**. 종합점수 분포는 중위 0.32 · p90 1.01 · p99 1.45 · **관측 최고 1.91**인데
그때의 유효 매수문턱은 **2.0~2.4**였다(기본 1.2 + 조정/약세 0.4~0.8 + 거시 비우호 0.3 +
`smart_net_20d −18.3조` → 0.5). 여기에 봇 성향 문턱(보수 1.9 / 균형 1.6)이 또 붙는다.
즉 **문턱이 점수 분포 밖**이라 매수 0건은 판단이 아니라 산수였다. `strong_buy_threshold=2.0`은
2,000건 중 도달 0건 — 사실상 죽은 코드였다.

점수는 8팩터 가중평균 × 3이라 2.0을 넘으려면 정규화 팩터 평균이 0.67이어야 한다. 강세장에서도
거의 안 생기는 조건이므로, 이건 시장 상태가 아니라 **캘리브레이션 결함**이다.

**바꾼 것**

| 축 | 이전 | 이후 |
|---|---|---|
| 매수권 | 절대 문턱(`buy_threshold`) | **횡단면 분위** `selection_mode="rank"` · 상위 `rank_top_pct`(기본 3% = 200종목 중 6자리) + 최소점수 `rank_min_score=1.2`(`buy_threshold` 동기) · 창 밖 채우기 금지 |
| 국면 | 매수 문턱 상향(자격) | **총 익스포저**(크기) `regime.target_exposure` — 강세 1.0 / 중립 0.7 / 과열 0.6 / 약세 0.4 / 조정 0.2, 거시·수급은 곱연산, **하한 0.15** |
| 추세 게이트 | 역배열이면 무조건 BUY→HOLD | **상대 기준** — 시장 20일 수익 중위값보다 덜 빠졌으면 게이트 완화(`_downtrend_blocking`) |
| 봇 성향 | `min_buy_score` 절대값 1.3~1.9 | 분위 폭(보수 1% / 균형 2% / 공격 3%) — 절대값은 `absolute` 모드에서만 |
| 매도 | 절대 기준 | **그대로**(청산은 억제하지 않는다) |

- 매수권 k자리는 순위를 내려가며 채운다 — 상위에 악재·게이트 종목이 있으면 다음 순위가 올라온다.
  자리를 비워두면 veto 몇 건이 조용히 후보 수를 깎아 원래 버그와 같은 결과가 된다.
- 실제 7/24 스냅샷 재계산: 매수 0건 → **6종목**(005440 1.82 · 000990 1.71 · 015760 1.40 ·
  002380 1.40 · 267250 1.30 · 009970 1.29), 컷오프 1.29. 지금 국면·수급이면 익스포저 15~19%.
- `absolute` 모드는 남겨뒀고(관리자 전환 가능), 그 모드에서 `max < 문턱`이면 관리자 화면에
  **「매수가 산술적으로 불가능합니다」**를 띄운다 — 같은 실패가 조용히 반복되지 않게.

**전제로 확인해야 할 것**: 로컬 PIT의 일간 순위상관이 0.988~1.000이고 technical·momentum의
일간 표준편차가 0.0000이었다(로컬 `prices.parquet`이 7/3자로 정지 — 프로덕션 여부는 미확인).
시세가 멈추면 분위든 문턱이든 매일 같은 결과가 나오므로 `store.signal_drift()` +
`/api/data-health`에 **점수 동결 감지**를 붙였다. 관리자 → 데이터 진단에서 먼저 확인할 것.

**아직 안 한 것 (다음)**
- 숏·인버스 헤지(하락장에서 실제로 버는 경로) — 매도 정밀도 표본 확보 후.
- 짧은 보유(3~10일) 평균회귀 전략: 변동성이 높을수록 유리해 하락장에 맞지만, 하네스가
  `판별력 있음`을 내기 전에는 미착수(아래 절 참고).

## 포트폴리오 하네스 — 측정: 아직 미입증, 다만 신호는 옮겨왔다 (2026-07-26)

`sigdesk harness` 신설(`signals/harness.py`). 전체 결과·방법론은
[docs/harness-findings.md](docs/harness-findings.md).

**1차(400일 데이터)**: 8개 조합 전부 `판정 불가`, 초과수익 7개가 음수. 병목은 표본 부족이었다 —
리밸런스 27회에 위상 편차(38~219pp)가 초과수익(−16~+7pp)을 압도했다.

**2차(5년 데이터)**: 이력을 1,221거래일로 늘린 뒤 8개 중 7개가 백분위 91~100%로 올라왔고,
10%/20일 하나가 `판별력 있음`. **그래도 채택하지 않는다** — 8개를 한 번에 보면 판별력이 전혀
없어도 하나가 95%를 넘을 확률이 34%다. 초록 한 칸을 고르는 건 측정이 아니라 고르기다. 남은
장벽은 위상편차(138~1,022pp)로, 좁은 분위일수록 달력 운에 좌우된다.

**방법**: 생존편향을 제거할 수 없으므로(과거 편입 이력 없음 — §0) 절대 수익률은 판단에 쓰지
않고, 대조군 대비 백분위로 판단한다. 리밸런스 위상을 전부 돌려 평균·최소·최대를 내고,
**가장 나쁜 위상까지 대조군을 이겨야** 인정한다(6거래일 미뤘더니 −4.7% → +61.3%로 뒤집힌
사례가 이 규칙의 이유).

**대조군 교체 — 회전율이 만든 가짜 판별력**: 5년 데이터에 셔플 검사(정보량 0)를 돌렸더니
백분위 100%로 `판별력 있음`이 나왔다. 옛 대조군은 매 기간 k종목을 새로 뽑아 회전율 100%로
거래비용을 최대로 물었고, 점수 기반 전략은 점수가 지속적이라 덜 물었다 — **비용 차이만으로
이긴 것**이다(부의 배수 0.503 vs 0.869). 지금은 **같은 시뮬레이터에 라벨만 치환한 점수**를 넣어
대조군을 만든다. 이 버그가 기존 셔플 검사를 빠져나간 이유는 검사를 `cost_pct=0`으로 돌렸기
때문이고, 비용을 켠 검사 2종을 레드팀에 추가했다.

**커버리지 — '계산 불가'와 '조건 미발동' 분리**: 낙폭과대는 급락 때만 가중치를 갖는 조건부
팩터라 5년치를 넣어도 발동률 2.5%다. 이걸 이력 부족으로 세면 데이터를 더 받아도 판정이 영원히
막힌다. 차단 사유는 이력 부족뿐으로 좁히고, 낮은 발동률은 경고로만 남긴다. 모멘텀은 5년 확보로
커버리지 5.9% → 100%가 됐다.

**하네스 자체 버그 4건**을 잡고서야 위 숫자가 나왔다: 매수 0건 기간에 거래비용 차감 / 동점을
유니버스(시총) 순으로 정렬 / 대조군만 단일 위상 / 대조군 회전율 불일치.
`tests/test_harness.py`의 양성 대조군이 `판별력 있음`을 내는지 먼저 확인한다 — 깨지면
`판정 불가`는 발견이 아니라 도구 고장이다.

**결정**: 판별력이 사전에 정한 조합에서 재현될 때까지 가중치·부호·분위를 바꾸지 않고, 페이퍼 봇
회전율도 더 열지 않는다. 하네스는 리포트가 아니라 **변경을 막는 게이트**로 쓴다.

## 시세 자동 갱신 결함 — 수집 코드는 있는데 아무도 안 불렀다 (2026-07-26)

정확도 전 항목이 null이던 원인을 따라가보니 수집 실패가 아니라 **호출 부재**였다.
`store.fetch_prices` 호출처가 CLI와 관리자 '데이터 갱신' 버튼뿐이고 일일 루프에 없었다. 장중
루프(`_quote_loop`)는 현재가만 갱신하고 일봉 이력(`prices.parquet`)은 건드리지 않는다. 그래서
시세는 07-03에 멈춘 채 시그널만 07-24까지 쌓였다.

깊이도 같이 막혀 있었다. `prices_deep_backfilled` 불리언 래치 때문에 목표를 5년으로 올린 뒤에도
400일치에 머물렀다(197종목이 전부 같은 날짜에서 시작).

**고친 것**
- 마감후 일일 루프에 시세 갱신 추가. **봇 사용자 유무와 무관하게** 돈다 — 데이터 신선도가 봇
  활성화에 딸려 있으면 안 된다(`api._daily_maintenance`).
- 완료 플래그 대신 **실제 저장 깊이를 재서** 백필 여부를 정한다(`store.prices_need_deep_backfill`).
  목표를 올리면 다음 갱신에서 스스로 다시 채운다.
- 회귀 가드 `tests/test_price_freshness.py` 8건.

**확보한 데이터**: pykrx 종목별 일봉은 무료·무키에 5.5년을 0.3초/종목으로 준다. 200종목 5년
전량 백필이 **14초**, 53,252행 → 232,198행(2021-07-27~2026-07-24). "3년 확보"는 새 소스를 구할
문제가 아니라 백필을 한 번 돌리는 문제였다.

**남은 것 — 생존편향(§0과 연결)**: 시점별 편입 종목을 얻으려면 pykrx 벌크 엔드포인트(시총 랭킹·
상장종목 리스트·PER/PBR)가 필요한데 지금 전부 빈 응답이다. 원인은 KRX가 로그인을 요구하도록
바뀐 것으로 보이고, pykrx에 이미 로그인 모듈이 들어와 있다(`KRX_ID`/`KRX_PW` 환경변수,
data.krx.co.kr 무료 가입). 계정만 있으면 상장폐지 종목 포함 PIT 유니버스를 만들 수 있는지
확인 가능 — **사용자 액션 필요, 미검증**.

## 데이터 흐름 진단 — 죽은 게이트·갈라진 점수·해시 벡터 (2026-07-27)

"수집한 데이터가 적재적소에 쓰이고 있나"를 시그널 수명주기·두뇌 레이어·KB 세 갈래로 점검한
결과. 라이브 점수 경로(시세·재무·수급·공매도 + 국면 게이트)는 유기적이었고, 문제는 **경로 밖에서
조용히 비어 있던 것들**이었다.

**고친 것**

| 발견 | 실체 | 대응 |
|---|---|---|
| 투자경고 veto가 한 번도 발동 안 함 | `fetch_warnings` 호출처가 관리자 수동뿐 → `warnings.json` 부재 → `load_warned_tickers()` 항상 빈 집합 | 일일 루프에 추가 + `store.warnings_status()`로 '정상 0/미수집 0' 구분 노출 |
| 봇과 화면의 점수가 다름 | 봇 국내 경로가 `evaluate`에 `shorts` 미전달(가중치 0.15) | 입력을 `store.kr_engine_inputs()` 한 벌로 공유 + `evaluate` 시그니처 대조 검사 |
| PIT 스냅샷 오염 가능 | 15:40 스냅샷 시점에 장중가 오버레이가 남으면 장중가 점수가 저장되는데 채점은 종가 | 스냅샷 직전 오버레이 해제 + 거래일 KST |
| 시세 실패 시 낡은 장중가 고정 | 예외·빈 응답 경로가 오버레이를 안 걷음 | 실패 시 종가 복귀 + 캐시 무효화 |
| "KB 벡터화" 실체가 해시 | 297건 전부 `hashing-v1`, `semantic_capable()=False` — 동의어를 못 잡음 | `[embed]` extra로 e5-small 전환·재임베딩, 백엔드를 관리자 화면에 노출 |
| 임베딩 문턱이 백엔드와 안 맞음 | 절대 cosine 0.78이 e5에선 전부 오탐(중립 0.891~0.916 vs 악재 0.900~0.957로 겹침) | 중립 앵커 대비 **마진** 판정 + 검색은 BM25가 후보를 정하고 dense는 재정렬만 |
| shadow에 판정 규칙 없음 | climate는 diverge 건수만, 컨센서스는 축적만 | `accuracy.diff_verdict`(유의성 기반) 공유 + `climate.shadow_verdict` + `store.consensus_readiness`(측정 가능 날짜·ETA) |

**이어서 발견 — 종목 KB 수집이 7일 멈춰 있었다 (2026-07-27)**

원문 문서 보존 규칙(`db.kb_prune`: 뉴스 종목당 30건·90일, pending 14일, 인사이트 60건·180일)은
정상이었지만 **호출처가 `kb.refresh()`의 마지막 한 줄뿐**이었고, 그 루프에 per-target 예외 처리가
없었다. 한 종목 수집이 던지면 나머지 종목 다이제스트·prune·증분 임베드가 전부 스킵된다.
실제 상태: `naver_news` 최신 07-20, 국내 다이제스트 최고 24일 경과, 인사이트 원문 107건(한도 60).
수동으로 한 번 돌리자 **인사이트 47건 + 고아 임베딩 47건이 즉시 삭제**됐다.

가려진 이유는 신선도를 전체 다이제스트 `max(updated)`로 쟀기 때문이다 — 거시·US는 매일 갱신돼서
화면상 '0.2시간 전'이었다.

| 대응 | 내용 |
|---|---|
| 실패 격리 | `kb._refresh_one` 분리 + per-target try/except, 실패 종목 이름·예외형을 `kv:kb_refresh_last`에 남김 |
| 정리 보장 | prune·임베드는 실패와 무관하게 실행 |
| 정지 노출 | `kb.refresh_status()` — 수집 대상 중 신선한 종목 수·밀린 종목 이름·차단 이유. `/api/data-health`의 `kb_refresh` |
| 재발 방지 | `test_redteam.test_one_bad_ticker_does_not_stop_collection_or_pruning` |

**관리자 IA 재배치 (2026-07-27)**

엔진 탭 한 곳에 헬스·제안·실측·shadow·감사·D7·LLM비용이 세로로 쌓여 있었고, 데이터 진단은 버튼을
눌러야 나왔고, `/api/data-health`가 이미 돌려주던 `warnings_veto`·`kb_retrieval`·`consensus_readiness`는
**화면에 렌더되지 않고 있었다**(shadow API 2개도 UI 없음).

- 서브탭 5축: **점검 / 엔진 / 성적 / 뉴스 / 발행** (구 운영·데이터·검증·실측·KB·콘텐츠).
  옛 해시(`#admin/shortform`, `#admin/watch`)는 별칭으로 새 탭에 매핑.
- **오늘 할 일** 스트립 + 배지 스마트 랜딩. 비용·D7·엔진그래프·관측/감사·원문추가는 접기.
- 점검: 데이터 상태 + 「조용한 0 점검」 표 + 수집(접기) + LLM·D7(접기).
- 성적: 실측 · 정성 승격 · shadow 판정 · 감사(접기). 판정 대기면 `판정` 배지.
- 뉴스: 이벤트 기본 뷰. 소스·원문 추가는 접기.
- 엔진은 **개선 제안**을 먼저, 건강 그래프는 접기.

**KB 게이트는 예측력을 보증하지 않는다 → 커버리지 shadow (2026-07-27)**

"강한 게이트를 통과했으니 믿을만한 인사이트"인지 코드로 확인한 결과, 게이트가 보는 것은
**출처 등록·활성·scope + LLM 품질심사(광고·스팸·무관·허위 차단)** 이고 소스 품질 점수도
`수락률 − 거절률`이다 — 즉 **형식적 신뢰도**이고 주장이 맞았는지는 한 번도 채점되지 않았다.
재고도 한쪽으로 쏠려 있다: 인사이트 대부분이 `_MARKET`(거시·시황)이고 **국내 종목에 붙은 전문가
인사이트는 0건**, 원문 보유 종목은 17/200(8.5%).

그래서 LLM 비용 0으로 첫 채점을 붙였다 — `signals/kb_coverage.py`:
- 같은 날 **매수 판정 종목**을 원문 3건 이상 vs 0건으로 갈라 20거래일 실현수익률 차이를
  `accuracy.diff_verdict`(유의성 공용 규칙)로 판정. 점수·봇 미반영(shadow).
- 문서 수는 **PIT로 기록**한다(`snapshot_signals`의 `kb_docs` 열). 나중에 `fetched`로 재구성하면
  prune이 지운 문서만큼 과소집계되므로, 과거 행만 재구성값을 쓰고 **재구성 행 수를 함께 보고**한다.
- 편향은 응답의 `caveats`로 화면에 같이 낸다 — 수집 대상이 매수권·보유·관심 우선이라 문서 유무에는
  "우리가 수집했나"가 섞여 있다. 정보의 인과가 아니라 **정보가 붙는 종목의 사후 성과 차이**다.
- 현재 판정: 매수 판정 스냅샷 행이 1개라 `판정 불가`(이유 노출). 순위 기준 전환 후 새로 쌓이는 중.

수집 대상도 넓혔다(`_kb_targets`): `kind=="BUY"` 문자열 비교가 **우선매수(STRONG_BUY)를 빠뜨리고
있었고**, 매수권만 보면 정보 유무 비교 자체가 불가능하다 → `is_buy` + **점수 상위(문턱 근접)** 24종목
포함. 대상 25 → 42종목. KB 다이제스트·후보 이벤트 LLM 호출이 그만큼 늘어나므로 운영·데이터 탭의
LLM 비용 카드로 관찰한다.

**남은 것**
- `kb_events` 0건 — KB가 매매에 닿는 유일한 선(DART confirmed → Decision veto)에 데이터가 없다.
  위 수집 정지·좁은 대상이 원인의 일부이므로 정상화 후 재측정.
- 주장 원장(claim ledger) — 인사이트에서 "대상·방향·기간"을 구조화 추출해 자동 채점하면 소스별
  적중률을 base rate 대비 리프트로 쌓을 수 있다. 커버리지 shadow가 먼저 판정되면 착수.
- 합의·이견 지도 — 주장 태그 집계로 합의 강도/이견 구간을 관측. 채점기는 위 원장을 재사용.
- 컨센서스 PIT는 여전히 미반영. `consensus_readiness`가 ready를 띄우면 리비전 팩터 IC부터 잰다.
- 정성(KB) 점수는 combine 밖 유지(설계). 승격 게이트의 `priority`/`threshold`는 미구현.
- LLM 지출 중 시그널·봇에 닿지 않는 것들(audit·hypothesis·about/moves·narrative) — 관측·표시
  가치는 있으나 비용 대비 소비처를 한 번 정리할 필요.

## US 클래스주 심볼 불일치 + 무한 재시도 (2026-07-26)

로그를 30분마다 채우던 `BRK-B`/`BF-B` 오류의 원인. S&P500 원본은 `BRK.B`/`BF.B`인데
`sp500_constituents()`가 `.` → `-`로 바꿔 저장하고(주석엔 "KIS 심볼 표기"라고 적혀 있었지만
근거 없음) 그 표기를 그대로 외부에 물었다 → 토스 404(stock-not-found), KIS 세 거래소 모두 탐지
실패. 503종목 중 이 2종목만 영구 실패였고, 실패를 기억하지 않아 `missing[:25]` 앞자리를 계속
차지한 채 루프마다 재시도했다(토스 4회 + KIS 최대 9회).

**고친 것**
- 내부 ID(`BRK-B`)는 그대로 두고 — 관심종목·보유·parquet가 이 형태다 — 외부 호출만
  후보 표기(`BRK.B` → `BRK-B` → `BRKB` → `BRK/B`)를 순서대로 시도하고 통한 표기를
  `us_symbols.json`에 제공자별로 캐시(`us.symbol_variants`). 대시 없는 티커는 후보가 하나라
  나머지 499종목 경로는 그대로다.
- 연속 실패 3회면 7일 유예(`us_price_skip.json`). 자동 루프만 유예하고 관리자 '데이터 갱신'은
  무시한다. 유예 목록은 마감후 하루 한 번 이름과 함께 로그.
- 실패 로그에 시도한 표기와 연속 실패 횟수를 붙였다(이전: `US 거래소 탐지 실패, 제외: BF-B`).
- `tests/test_us_symbols.py` 9건.

**미검증**: 로컬에 토스·KIS 자격증명이 없어 어느 표기가 실제로 통하는지 확인하지 못했다. 점 표기가
정답이라 단정하지 않고 후보를 시도하는 구조로 만든 이유다. 배포 후 `us_symbols.json`에 무엇이
캐시됐는지 보면 확정된다. 4종목이 아직 누락(`BRK-B`, `BF-B`, `MKC`, `MCD`) — 뒤 2개는 표기
문제가 아니라 일시 실패로 보인다.

## 검증 시스템 3층 — 판정은 기계, 가설은 에이전트 (2026-07-26)

전체 설계·근거는 [docs/verification-system.md](docs/verification-system.md).

- **0층 CI** (`.github/workflows/ci.yml`) — 그동안 테스트 657개가 아무것도 막지 못했다.
  이제 PR·main 푸시에서 전체 + 레드팀 스위트를 돌린다.
- **1층 레드팀** (`tests/test_redteam.py`) — 셔플(누수)·음성 대조군(랜덤워크)·양성 대조군·
  미래 불변성(전 replay 경로)·base rate 동반·표본/커버리지 차단. 하네스에 `--shuffle`,
  `min_periods=30`, 커버리지 60% 미만 판정 차단 추가.
- **2층 감사 가설** (`audit.py` · 관리자 엔진 탭) — LLM은 **반증 가능한 의심만** 쓰고 판정·제안
  금지. 반증 방법 없는 항목은 저장 안 됨, 상태 전환은 사람만, 엔진 영향 0(테스트로 못박음).

**첫 실행에서 실제 발견**: `rows=2000`인데 `tickers_matched=0`. 기계로 확인하니 티커 조인은
멀쩡했고(200/200), 진짜 원인은 **시세 캐시가 07-03에서 멈춘 채 시그널만 07-24까지 쌓인 것**.
시세가 시그널보다 과거라 어떤 신호도 성숙할 수 없는데 화면은 "track record 누적 중"이었다.
→ `accuracy.coverage`에 `price_data_to`·`stale_prices`·`blocked_reason` 추가해 0의 이유를
말하게 하고, `test_zero_matured_says_why_it_is_zero`로 승격.

**미착수**: 주기 실행(큐가 소음이 되면 아무도 안 본다 — 큰 변경 뒤 수동 실행이 지금 규모에 맞다).

## Desk Hierarchy — 권한 사다리 (2026-07-31)

정본: [docs/desk-hierarchy.md](docs/desk-hierarchy.md) · Lx: [docs/product-reviewer.md](docs/product-reviewer.md).

에이전트 부서 풀빌드가 아니라 **위로 갈수록 기권·제거만** 하는 권한 위계.
- ✅ L4 Desk Report 템플릿 (`signals/desk_report.py` · `/api/signals` · 시그널 탭 접기)
- ✅ 미분류 쏠림 ≠ crowded (`crowding.data_quality`)
- ✅ Lx Product Reviewer 스케치 (`product_reviewer.py` · `POST /api/product-review/run`)
- ✅ 시그널 UI 밀도 — 「오늘」카드로 precision·편중·데스크 병합 · trust 기본 접기 · 리스트/히어로 축약
- ⬜ Lx 야간 자동 스케줄 · 타사 provider 교차검열
- ⬜ L1 역할분리 LLM / L2 다라운드 — **shadow 유의 승리면만** (지금 보류)

### 리뷰보드 Now 잔여 (2026-07-22)

| 항목 | 상태 |
|---|---|
| 매수 0일 히어로 · 신뢰 스트립 페이퍼 강등 | ✅ #258 |
| 시장 ZONE ↔ 경기 사이클 라벨 분리 | ✅ |
| 스크리너 a11y · 모바일 필터 드로어 | ✅ |
| 근접→페이퍼 CTA | ✅ |
| 기후 shadow 일별 스냅샷(`/api/climate-shadow`) | ✅ 관측만 · 승격 아님 |
| advisor shadow(`/api/advisor-shadow`) | ✅ 관측만 · 선별 로직 미변경 |
| 아침 브리핑(텔레그램 채널, 평일 07시) | ✅ `digest.py` · 매수 0일에도 발송 · 복귀 링크·전일 대비 |
| 북극성 D7 계측 + 관리자 카드 | ✅ `signal_visits` · `/api/d7` |
| 매수권 캘리브레이션(분위 전환·익스포저) | ✅ 2026-07-26 · 위 절 참고 |
| 정직한 백테스트 하네스(대조군·위상평균·룩어헤드 차단) | ✅ 2026-07-26 · `sigdesk harness` · 5년 데이터로도 채택 기준 미달 |
| 시세 5년 백필 + 일일 자동 갱신 | ✅ 2026-07-26 · 위 절 참고 |
| 검증 시스템 3층(CI·레드팀·감사 가설) | ✅ 2026-07-26 · 위 절 참고 |

## MVP prod 배포 체크리스트 (2026-07-05 기획 점검 — "후속까지 Phase2 포함 배포" 결정)

가족 내부 사용 전제(사용자 확정) — 규제 부담 낮음, 법률 검토는 사용자 별도 트랙.

| 항목 | 상태 | 비고 |
|---|---|---|
| 실데이터 재적재·배율 검증 | ⬜ **사용자 트랙(prod env)** | 샌드박스 시세가 종목별 스케일 → prod 실 KRX/증권사 피드로 재적재 전까지 백테스트·저평가·시나리오 절대값 신뢰불가. [[signal-desk-scaled-market-data]] |
| 법률 검토(유사투자자문·약관·개인정보) | ⬜ **사용자 트랙** | 가족 사용이라 후순위. 유료화 시 재검토 |
| 면책·약관·개인정보 UI 스캐폴딩 | ✅ #50 | 면책 상시 footer + 약관/개인정보/유의사항 모달(문구는 운영자 확정 초안) |
| 배포 인프라(Dockerfile·README·HTTPS·백업) | ✅ #52 | Dockerfile + README 배포가이드 + 운영 환경변수표 |
| 외부 스케줄러(봇+KB수집) | ✅ #52 | 서버 in-process 루프에 KB 하루1회 자동수집 추가 + 컨테이너 상시기동(restart 정책)으로 상시화 |
| 인증 하드닝(secure 쿠키·레이트리밋) | ✅ #49 | prod secure 쿠키 + 로그인/가입 레이트리밋(429) |
| 자동매매 안전장치(kill switch·일일손실한도·실계좌 가드) | ✅ #48 | 실주문(B) 켜기 전 필수 — 3중 게이트 |
| 신규 유저 빈 상태 가이드 | ✅ #51 | 보유종목 0이면 unlock 안내 |
| 모바일 반응형 점검 | 🔶 #51 부분 | 카드 가로스크롤 등 가드 추가, 세부 실기기 QA는 배포 후 |
| US 시총·PER(Alpha Vantage) | ✅ #53 | 주식수 캐시(20/일 백필)+시총 재계산. prod에 AV 키 필요, S&P500 전량 ~25일 |
| KB 수집 fanding tt 토큰 갱신 절차 | ⬜ 운영 | 세션토큰 만료 시 .env 갱신 |
| 비밀번호 재설정·이메일 인증 | ⬜ 후속(SMTP 필요) | 가족 사용이라 후순위 |

### 유료화 게이트 — 관리자 계정관리 (기록만, 당장 미구현)
나중에 유료화 시 **다른 플랫폼에서 결제 확인 → 사용자가 허용한 계정만 서비스 온전히 이용**하도록,
관리자 메뉴에 **계정관리(활성화/비활성화 화이트리스트)** 기능 추가 예정. 미허용 계정은 로그인은
되되 시그널·자동매매 등 핵심 기능 제한. (사용자 확정 2026-07-05 — 당장은 불필요, 백로그 기록만)

## 개인 페이퍼 봇 제거 — 리셋할 수 있는 장부는 증거가 아니다 (2026-07-27)

유저별 페이퍼 봇(켜기·시드·성향·초기화·수동실행·예약)을 걷어내고, 페이퍼 경로를 **레퍼런스 3봇
공개 장부(읽기 전용)** 로 좁혔다.

**왜 뺐나**
1. **리셋 버튼이 track record를 무효로 만든다.** 시드·성향 변경과 초기화가 유저 손에 있으면 성적이
   나쁜 장부는 사라지고 좋은 장부만 남는다 — 백테스트에서 잡아낸 생존편향과 같은 병이다.
2. **격리가 실제로는 깨져 있었다.** `bot_decisions`에 소유자 컬럼이 없어 모든 봇의 판단이 한 저널에
   섞였고, 그 저널이 전체 유저에게 보이는 신뢰 스트립 승률과 LLM 매수 선별 맥락
   (`advisor.build_lessons`)에 들어갔다. 유저의 연습 매매가 다른 봇의 매수에 스며드는 경로였다.
3. **아무도 안 썼다.** 개인 봇 5개(uid 3·4·21·22·33) 전부 OFF, 체결 0건. 돌던 건 레퍼런스 3봇뿐.
4. 유저가 얻으려던 정보("시그널을 따라가면 어떻게 되나")는 리셋 없이 계속 쌓이는 공개 장부가 더
   길고 정직하게 준다.

**같이 고친 것**
- 중복 계수: 성향별 3봇이 같은 종목을 사면 같은 판단이 3번 기록돼 승률이 종목 인기도로 가중됐다
  (39건 중 고유 31건). 판단 단위로 접자 22.2% → 33.3%. `deduped_from`으로 몇 건 접혔는지 노출.
- 알림 결합 해제: 관심종목 시그널 변동 알림 스캔 대상이 '봇 켠 유저'였다 → '관심종목 있는 유저'.
- 1회 마이그레이션으로 개인 uid의 봇 행·페이퍼 kv 정리(`db._drop_personal_paper_accounts`).

**남긴 것**: 레퍼런스 3봇(900001~3)의 자동 운용, advisor shadow 기록, 숏폼 성과 소재, 공개 장부 UI.

**후속**: 공개 장부 성적이 성숙하면 성향별 승률·MDD를 랜딩/세일즈 자료로 노출할지 결정.

## 자동매매봇 후속 작업 (범위 밖으로 미룬 것)

- 주문 체결 상태 폴링 — 모의투자 즉시체결을 가정, 부분체결·미체결 처리 없음.
- 동일가중 이상의 정교한 포지션 사이징(변동성 조정 등).
- 시간별 총평가금액 스냅샷 기반 자산곡선 차트.
- 가격 급등락 알림 + 외부 알림 채널(이메일/푸시) — 현재 인앱 시그널변동 알림만(#16).
