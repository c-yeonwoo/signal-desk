# CLAUDE.md — signal-desk

개인 R&D용 시스템 트레이딩 랩. 시그널 판별력(IC·shadow·harness + 이유 재생)을
1순위로 두고, 페이퍼 봇으로 타이밍을 검증하며 알고리즘을 디벨롭한다.
외부 소비자 성장이 1순위가 아니다 —
[docs/north-star-selection.md](docs/north-star-selection.md).
자매 [Signal APT](https://github.com/c-yeonwoo/apt-signal) 아키텍처를 주식으로 재해석.
체리픽: [NOTES-cherrypick.md](NOTES-cherrypick.md).

## 무엇인가

**거시 사이클 → 산업 밸류체인 → 저평가(스크리너) → 시그널(타이밍) → 페이퍼 자동매매** 로
이어지는 투자 판단 흐름. UI는 4탭(+관리자)으로 압축:

| 탭 | 역할 |
|---|---|
| 시그널 | 8팩터+게이트 종합 시그널 · 차트 · 스크리너 · 매수 대기 · 신뢰 스트립 |
| 페이퍼 | 공개 장부(레퍼런스 3봇, 읽기 전용) · 실보유/리밸런싱 · 배당 |
| 인사이트 | 사이클 · 밸류체인 · 학습 · 거장 · ETF (**맥락**, 신호 아님) |
| 관리자 | KB · 숏폼 · 엔진 가중/실측/두뇌 |

핵심 시너지: 사이클 주도섹터 → 밸류체인 → 국내기업 → 시그널 딥링크. 신뢰 해자는
**실측 track record**(백테스트 시뮬과 구분). 저평가·후보는 시그널 스크리너/opp_tags로 흡수.
## 기술 스택

- **Python 3.11~3.13** (3.14 금지 — Signal APT에서 pandas/pyarrow datetime 추론 세그폴트
  exit 139 이력. 이 리포는 아직 pandas 미도입이라 즉시 리스크는 없지만 2단계부터 적용)
- FastAPI + uvicorn, typer(CLI), ECharts(CDN, 프론트 차트)
- 프론트: 단일 파일 `src/signal_desk/web/index.html` (인라인 vanilla JS SPA, 해시 라우팅)
- 인증: 표준 라이브러리 pbkdf2 세션(`auth.py`) — 외부 의존성 0
- 데이터: SQLite(`data/cache/app.db`) — 2단계부터 parquet/json 캐시 추가 예정

## 빌드 & 실행

```bash
python3.12 -m venv .venv && .venv/bin/pip install -e ".[dev]"
.venv/bin/sigdesk serve       # http://127.0.0.1:8765
.venv/bin/pytest -q
```

테스트는 루트 `conftest.py`가 외부 API 키를 빈 값으로 고정한다(오프라인·결정론). 키가 있는
경로를 검증할 땐 그 테스트에서 `monkeypatch.setenv`로 켤 것 — `.env`를 그대로 타면 실제 API가
호출된다(`api.py` import 시 `config.load_env()`).

## 현재 상태

**R&D 랩 (시그널 판별력 = A)**
- [x] 8팩터 엔진 + 게이트 + PIT 실측 + harness/shadow/redteam
- [x] 시그널 판별력 `GET /api/proof` · 픽 이유 `GET /api/pick-reason` · PIT reasons/gate
- [x] 페이퍼 레퍼런스 봇(시그널·타이밍 증명용, PnL은 종속 지표)
- [ ] A 유의 개선 후에만 가중·매수권 재확정 · qualitative priority 승격
- 동결: 온보딩/D7/숏폼 성장 — [BACKLOG.md](BACKLOG.md)

다음에 붙일 기능의 상세 우선순위·범위·의존관계는 [BACKLOG.md](BACKLOG.md) 참고.
## 핵심 개념 (부동산 → 주식 번역, 요약)

| Signal APT | Signal Desk |
|---|---|
| 지역(region) | 섹터/테마 |
| 단지(complex) | 종목(ticker) |
| 급지(A~D) | 시총 등급 / 밸류에이션 분위 |
| 경기순환 국면 | 시장 국면(강세·과열·조정·약세) |
| 저평가(입지 대비 가격) | 저평가(PER·PBR·성장 대비) |
| 매물 유형(경매·급매·청약) | 후보 유형(눌림목·낙폭과대·IPO·실적서프라이즈·턴어라운드) |
| 지도(Leaflet) | 섹터/테마 트리맵·히트맵(지리 없음) |
| 규제지역 오버레이 | 이벤트 오버레이(관리종목·투자경고·공매도과열·공시) |

자세한 아키텍처 대응은 노션 "Signal APT 완전 정복" 빌드 로그(2026-07-02) 참고.

## 데이터 규칙

- `data/raw/*`, `data/cache/*`는 커밋 금지(.gitignore 처리됨). 실제 API 키 커밋 금지 —
  `.env.example`에만 키 이름 문서화.
- 시그널 임계값·가중치는 하드코딩 금지, 설정 모듈 한 곳에 모을 것(2단계 `signals/engine.py`).
- `println`/`print` 디버그 금지 — 로거 사용.

## 지표 규칙 (2026-07-25 진단에서 실제로 틀렸던 것)

- **base rate 없는 비율은 노출 금지.** 정밀도·적중률·일치율·전환율을 UI·브리핑·API에 내보낼 때는
 **기준선(naive 예측기 성능)과 그 대비 리프트를 항상 함께** 낸다. 색·판정도 절대값이 아니라
 리프트로 한다. −3% 조정장에서는 무작위 매도도 정밀도 60%가 나오는데, 절대값 55%를 초록으로
 칠하고 있었다(`signals/accuracy.py`의 `baseline`·`*_lift_pp`·`MIN_LIFT_PP`가 기준 구현).
 표본이 작으면 신뢰구간 반폭(`*_ci_pp`)도 함께 — 리프트가 오차 안이면 무정보라고 말해야 한다.
- **상관은 관측 단위를 먼저 정하고, 표본은 그 단위로 센다(2026-08-06).** `factor_ic`가 2,200행을
  **한 덩어리로 푼 pooled 상관**이었다. 팩터 IC는 "그 날 종목 간 순위상관"인데 날짜를 섞으면
  상승일엔 전 종목 수익이 양수라 **팩터 분포에 날짜별 수준 차이만 있어도 상관이 생긴다** —
  판별력이 아니라 시장 드리프트다(`test_redteam.test_cross_sectional_ic_is_not_fooled_by_mixing_dates`가
  pooled |ρ|>0.5, 횡단면 ≈0인 구조로 이걸 박는다). 그리고 최소 표본을 **행**으로 셌다(`_MIN_IC_SAMPLES=20`)
  — 하루치 200종목이 표본 200이 되어 문턱을 즉시 통과했고, 실제로 `short IC −0.148`이 **단 하루**
  (200/2200행)에서 나온 값이었다. 같은 파일 `store.py`에 "같은 날 200종목은 하나의 관측"이라
  적어 두고 IC만 반대로 세고 있었다. 이제 `accuracy.cross_sectional_ic`가 **날짜 단위**로 재고
  `_MIN_IC_DATES`로 막는다.
- **중첩 창의 t는 부풀려진다 — 보정하고, 독립 관측 수를 같이 낸다.** h거래일 수익률을 매일 계산하면
  창이 h−1일씩 겹쳐 IC 시계열이 자기상관을 갖는다. 실측 10일 h5에서 소박한 t는 momentum −5.18인데
  비중첩으로 세면 −1.62였다. Newey-West(Bartlett, lag=h−1)로 보정하고 `independent_dates = n/h`를
  **화면에 같이** 쓴다 — "10일"과 "10관측"을 혼동하면 20일 요건이 의미를 잃는다. 그리고 **SE 하한은
  소박한 SE**로 둔다(`se_floored`): 소표본에서 NW가 음의 자기상관을 추정해 iid보다 정밀하다고
  주장하는 경우가 있었다(reversion t −6.85 → −10.68). 통계적 항등식이 아니라 보수성 선택이므로
  적용 여부를 플래그로 남긴다.
- **IC를 숫자 하나로 내보내지 않는다.** 크기만 보이면 그게 판별력처럼 읽히고, 실제로 `momentum −0.341`이
  가중 0.30을 정당화하는 근거로 쓰였다. `n_dates`·`ci95`·`t`·`p`·`blocked_reason`을 항상 동반하고
  (`factor_ic_stats`), **요건 미달·무유의면 `factor_ic[k]`를 None으로 비운다** — 두 키가 어긋나면
  화면과 게이트가 갈라진다(`test_factor_ic_and_stats_never_disagree`). 가중치 제안은 IC 크기가 아니라
  `significant`를 본다(`brain_proposals.ic_usable`). p는 소표본에서 정규근사가 과소평가하므로
  Student-t로 낸다(`accuracy.t_two_sided_p` — 의존성 0, 임계표와 소수 4자리 일치).
- **비율을 비교하려면 관례를 먼저 통일한다(2026-08-05).** 봇 채점이 `closes[-1]`(오늘 종가)을 써서
  보유 기간이 "채점 루프가 실제로 돌 때까지"로 판단마다 달랐다(실측 3.0~6.1 달력일). 그 상태로
  `baseline_buy_pct`(익일 종가·정확히 5거래일)와 비교해 **"리프트 +0.4%p"라는 없는 숫자**를 만들었다.
  지평·진입/청산 관례·모집단·기간이 하나라도 다르면 리프트는 거짓이다. 이제 채점은
  `판단일 다음 거래일 종가 → h거래일 뒤 종가`(`bot.OUTCOME_HORIZON_DAYS`)로 고정하고 `horizon_days`를
  행에 남긴다. **지평이 없는 행은 리프트에서 빼고 몇 건을 뺐는지 밝힌다**(`mixed_horizon_n`) —
  안 밝히면 표본이 조용히 줄어든 것처럼 보인다. 청산일 종가가 없으면 **기다린다**(오늘 종가로 대신하면 옛 버그다).
- **0의 이유는 변호가 아니라 점검 결과여야 한다.** `매수 0 · 정밀도 우선 · 고장 아님`이 원인을 가리지
  않고 무조건 렌더됐다 — rank창(문턱 통과 31 vs 창 6자리)인지 게이트 차단(17)인지 수집 정지인지
  구분하지 못했다. 변호는 고장 조사를 막는다. 이유는 `selection.over_threshold`·`gate_blocked`·
  `stall_line` 같은 **실제 값**으로 쓴다.
- **shadow 관측은 '판정 알림'까지 만들어야 끝난다.** 데이터만 쌓고 사람이 들여다봐야 아는 관측은
 영원히 안 본다. shadow를 추가하면 (a) 판정 조건을 **표본 수가 아니라 유의성**으로 코드에 박고
 (`advisor_shadow.delta_significant`), (b) 조건 충족 시 관리자 화면·브리핑 중 한 곳에 뜨게 한다.
  「표본 N개 모이면 판정」은 통계적으로 성립하지 않는다 — 20쌍의 SE는 ±3.8%p다.
- **문턱은 점수 분포를 보고 정한다(2026-07-26).** 절대 임계값을 새로 만들거나 올릴 때는 그 값이
  실제 점수 분포의 어디인지(max·p99·중위)를 먼저 확인하고, 화면에도 분포와 함께 노출한다.
  매수문턱 2.0~2.4가 관측 최고점수 1.91보다 높아서 10거래일간 매수 1건이었다 — 신중함이 아니라
  0으로 나누기였다. **후보 집합이 비는 규칙보다 상대 순위(횡단면 분위)를 우선**한다.
- **국면·리스크는 자격이 아니라 크기로 반영한다.** "나쁜 장이니 안 산다"(자격 × 0)는 손실도
  학습도 없는 상태를 만들고, 페이퍼 track record가 영원히 안 쌓인다. 익스포저·비중을 줄이되
  **하한은 0보다 크게** 둔다(`regime.EXPOSURE_FLOOR`). 반대로 청산·매도 기준은 절대 기준 유지.
- **백테스트 숫자를 근거로 쓰기 전에 하네스부터 의심한다.** 유니버스가 "오늘 기준 상위 N종목"이면
  과거 모멘텀 상위 선택은 생존편향으로 연율 179%·MDD 0.0% 같은 값을 낸다. 시점별 유니버스·
  거래비용·룩어헤드 차단이 없으면 그 결과는 **성과의 증거가 아니라 편향의 증거**다.
- **대조군은 전략과 **같은 후보 집합**에서 골라야 한다(2026-08-05).** `_permuted_scores`가 점수
  시계열을 티커 간에 통째로 맞바꿨다. 종목마다 가용 날짜가 다르면(시점별 유니버스·PIT 재무)
  None 패턴까지 옮겨가 **대조군이 전략이 살 수 없던 종목**(이미 폐지돼 forward-fill 된 종목까지)을
  담는다. 그 0% 수익이 대조군을 끌어내려 전략이 좋아 보인다. 각 티커의 **자기 가용성은 유지하고
  값만 기증자에게서 받는다**(기증자가 그 날 비었으면 양방향 채움 — 대조군 점수는 정보가 없어야 하는
  값이라 낡아도 무해하다). 날짜별 후보 수가 전략과 같은지 **시드를 여러 개 돌려 검사**한다.
- **편향을 한 경로에서 없애면 나머지 경로를 전부 세어본다(2026-08-05).** 유니버스를 시점별로
  넓혔는데 재무·시총이 오늘의 200만 있어서, PIT 전용 105종목이 가격 3팩터로만 점수를 받고
  재정규화 편향으로 매수권 6자리 중 **평균 4.62자리**를 차지했다 — 백분위가 2.0(가짜 역판별력)이
  나왔다. 유니버스·재무·시총 커버리지가 갈라지면 그 차이가 그대로 팩터 편향이 된다.
  **결과가 극단으로 나오면 판별력을 의심하기 전에 커버리지를 센다.**
- **결측을 재정규화로 지우면 모르는 종목이 가장 센 점수를 받는다(2026-08-06).** `combine`의 분모가
  **발동한 가중치의 합**이라, 팩터가 빠진 종목은 남은 팩터로 재정규화된다. 실측 200종목에서
  `|점수|` 평균이 커버리지 순으로 **1.070 / 0.805 / 0.638 / 0.303** — 데이터가 가장 적은 종목이
  가장 센 점수를 받았고, 상위 3위가 전부 5/8 팩터였으며 **유일한 매수권 종목도 그랬다.**
  전 팩터를 가진 3종목은 최고 +0.43으로 사실상 매수 불가였다. 규칙: (a) `weight_sum_ratio`·
  `data_coverage`·`missing_factors`를 **산출물에 실어** 노출한다(숫자만 보면 커버리지가 안 보인다),
  (b) 커버리지 미달은 **매수권에서만** 뺀다 — 점수를 지우면 왜 없는지 모른다, (c) 분모를 전체
  가중합으로 바꾸는 것(결측=중립 0)이 편향의 정직한 해법이지만 **모든 점수가 변하므로** 판별력
  판정 전에는 하지 않고 하네스에 넣어 재는 것이 먼저다.
- **커버리지 문턱도 분포를 보고 정하고 분포와 함께 노출한다.** 실측 데이터 커버리지 분포는
  p5 0.759 · p25 0.862 · 중위 1.000이었다. 문턱 0.75는 **4종목만 막아 상위 6자리에 아무 영향이
  없었고**(=완화가 아무 것도 안 막는 경우), 0.90은 94종목·상위 6자리 전부를 막아 매수 0이었다(=0으로
  나누기). 0.80을 골랐다 — 27종목 · 상위 6 중 3자리. `selection.coverage`가 요건·차단 수·창 안
  차단 수·분포·없는 팩터를 함께 낸다.
- **커버리지를 셀 때 세 종류를 구분한다.** ① 데이터 없음(진짜 결함) ② **조건 미발동**(낙폭과대는
  급락 때만 발동 — `CONDITIONAL_FACTORS`) ③ **그 실행이 원리적으로 못 봄**(하네스의 수급·공매도는
  시계열 이력 자체가 없다 — `data_coverage(unavailable=)`). ②③을 ①로 세면 데이터를 아무리 더 받아도
  커버리지가 안 오르고, 가격 하네스는 전 종목 미달로 매수 0이 되어 게이트가 검증 불가가 된다.
- **게이트를 만들면 하네스에도 같이 넣는다(2026-08-06).** 커버리지 게이트를 라이브
  `apply_cross_sectional`에만 걸고 하네스 픽 루프에 안 걸면, 하네스는 **라이브가 돌리지 않는
  전략**을 잰다(`shorts` 누락·`signal_config` 미주입과 같은 병의 세 번째 재발). 그리고 **대조군에
  같은 커버리지 패널을 넘긴다** — 커버리지는 티커에 붙은 데이터 사실이라 라벨 치환과 무관하고,
  대조군만 게이트를 안 걸면 대조군이 더 많이 살 수 있어 그 차이가 판별력으로 둔갑한다.
  게이트가 실제로 걸렸는지·몇 번 막았는지를 결과에 싣는다(`data_coverage_gate`) — 0이면
  "있는 것처럼 보이지만 효과 없는 완화"다.
- **게이트로 막힌 자리를 다음 순위로 채우면 게이트가 없는 것과 같다(2026-08-06).** 하네스 픽 루프가
  `ranked` **전체**를 돌며 `len(picks) >= k` 에서 끊어, 라이브가 공석으로 두는 자리를 k 밖 종목으로
  채웠다. 그래서 추세 게이트가 **1076회** 걸렸는데도 켜고 끈 결과의 백분위·매수0기간·평균보유수가
  **완전히 같았다**(90.0 · 529 · 3.1) — 게이트가 사실상 재정렬이었다. 같은 날 라이브는 매수권
  0/6자리였다. 후보는 `ranked[:k]` 로 자르고 그 안에서만 거른다. 고친 뒤 백분위 **90.0 → 71.5** —
  **그전까지 잰 것은 라이브가 돌리지 않는 전략이었다.** 게이트를 하네스에 "넣었는지"가 아니라
  **막힌 뒤 무엇이 일어나는지**까지 같아야 한다.
- **게이트는 kind에 따라 다르게 동작하면 안 된다.** `_apply_crash_gate` 는 HOLD여도 `gated=True` 를
  세우는데(분위 승격이 급락 종목을 올리지 못하게) `_apply_trend_gate`·`_apply_earnings_gate` 는
  `kind not in BUY_KINDS` 면 early-return 했다. 게이트 3개, 정책 2개. `rank_min_score <
  buy_threshold` 인 구간의 점수는 게이트가 안 걸린 채 분위 승격으로 **STRONG_BUY가 됐고** 근거에
  추세 언급조차 없었다(재현: 하락추세 확인 종목이 점수 +0.80에 매수권). 두 문턱은 **둘 다 관리자가
  편집할 수 있다** — 지금 같아서 우연히 안 터졌을 뿐이다. `_mark_gate` 로 규약을 하나로 모았다.
- **`gate_blocked` 불리언 하나로는 매수 0을 설명할 수 없다.** 화면이 근거 문구를 `[추세]` 접두어로
  **문자열 파싱**해 태그를 만들고 있었고, 그 매핑은 어디에도 검사되지 않았다(문구를 고치면 태그가
  조용히 사라진다). 그리고 그 숫자는 **BUY 강등만** 세어 `추세 17종목` 이었는데 실제 추세 차단은
  **67종목**이었고, 완화는 7종목 발동했는데 화면상 0건이었다(완화를 BUY일 때만 기록했다).
  규칙: 게이트 발동은 `gates` 리스트로 **구조로** 남기고(`GATE_LABELS`), 완화도 같이 남기고,
  집계는 **전체 수와 상위 k자리 수를 따로** 낸다 — 전체 17이 창 6자리를 안 건드리면 매수 0의
  원인이 아니고, 실측은 반대였다(창 6자리 중 추세 5 · 커버리지 3).
- **점수에 영향을 주는 새 설정 필드는 `signalcfg.FIELDS`와 사전등록 `[base.config]`에 **둘 다** 넣는다.**
  `prereg.config_agrees_with_engine`은 **등록된 키만** 비교하므로, 새 필드를 등록에 안 넣으면 그
  파라미터는 검증된 적 없이 판정에 섞인다(`test_preregistered_config_covers_every_engine_field`).
- **부분 갱신이 나머지를 지우지 않는지 확인한다.** `fetch_fundamentals_history`가 대상 종목만
  받아 파일을 **통째로 덮어써** 기존 199종목이 사라졌다(#324의 `compute_quality`와 같은 병,
  이번엔 원본 쪽에서). 캐시를 쓰는 함수는 **기존을 읽어 병합**하고, 병합 전후 종목 수를 로그로 낸다.
- **편향을 없앨 수 없으면 같은 편향을 받는 대조군과 비교한다(2026-07-26).** 절대 수익률은 보고만
  하고 판단에 쓰지 않는다. 판단은 같은 날짜·같은 유니버스에서 무작위로 같은 수를 뽑는 몬테카를로
  대비 백분위로 한다(`signals/harness.py`). 그리고 **리밸런스 시작일(위상)을 전부 돌려** 평균과
  최소·최대를 함께 낸다 — 6거래일만 밀어도 5일 보유 누적이 −4.7% → +61.3%로 뒤집혔다.
  가장 나쁜 위상까지 대조군을 이겨야 판별력으로 인정하고, 아니면 **판정 불가가 기본값**이다.
- **대조군은 편향뿐 아니라 기계적 조건까지 공유해야 한다(2026-07-26).** 대조군을 별도 코드
  경로로 짜면 그 경로의 차이가 판별력으로 둔갑한다. 매 기간 k종목을 새로 뽑는 대조군은 회전율이
  늘 100%라 거래비용을 최대로 무는데, 점수 기반 전략은 점수가 지속적이라 덜 문다 — **정보량 0인
  셔플 전략이 백분위 100%로 `판별력 있음`을 받았다.** 대조군은 **같은 시뮬레이터에 라벨만 치환한
  점수**를 넣어 만든다(`harness._null_distribution`). 지속성·회전율·비용·게이트·보유 수가 같은
  분포로 유지되고 남는 차이가 점수의 정보량뿐일 때만 백분위가 p-value 구실을 한다.
- **검사는 현실과 같은 조건으로 돌린다.** 위 버그는 셔플 검사가 이미 있었는데도 빠져나갔다 —
  검사를 `cost_pct=0`으로 돌리고 있었기 때문이다. 비용이 0이면 회전율 차이가 결과에 안 나타난다.
  검사에서 편의상 끄는 조건 하나하나가 버그가 살 수 있는 틈이다.
- **조합을 여러 개 보면 하나는 우연히 통과한다.** 8개 조합을 한 번에 보면 판별력이 전혀 없어도
  그중 하나가 95%를 넘을 확률이 34%다. 스윕 결과에서 초록 한 칸을 골라 쓰는 건 측정이 아니라
  고르기다. 스윕 출력에 이 확률을 같이 찍고, 채택은 사전에 정한 조합으로만 한다.
- **팩터를 백테스트에 넣기 전에 커버리지부터 센다.** 이력이 모자란 팩터는 예외가 아니라 **가중치
  0으로 조용히 빠지고**, 표에는 그 팩터 이름이 붙은 빈 결과가 남는다. 모멘텀은 252거래일이
  필요한데 캐시가 268일이라 커버리지 5.9%였다 — "8팩터 백테스트"의 실체는 기술 팩터 하나였다.
  단 **'계산 불가'와 '조건 미발동'은 나눠 센다.** 낙폭과대는 급락 때만 가중치를 갖는 조건부
  팩터라 5년치를 넣어도 발동률이 2.5%다 — 이걸 이력 부족으로 세면 데이터를 아무리 더 받아도
  판정이 영원히 막힌다. 차단 사유는 이력 부족뿐이고, 낮은 발동률은 경고로만 남긴다.
- **증명 장치에 검증 대상을 넣을 수 있어야 한다(2026-08-05).** `store.run_harness`가 `HarnessConfig`를
  `signal_config` 없이 만들어 `default_factory=SignalConfig`가 걸렸다 — 화면·봇은 `signalcfg`를 쓰는데
  **판정만 소스 상수를 쟀다.** 오버라이드가 비어 우연히 일치했을 뿐이고, 가중치를 바꿔도 판정은 안 변했다.
  그래서 H1·H2는 `engine.py`를 편집하고 터미널에서 돌렸고 결과는 gitignore 밑에 남았다. 규칙: 검증 함수는
  **호출자가 넘긴 설정**을 재고(기본은 `signalcfg.get_config()`), 실험 파라미터를 **API·CLI로 넣을 수 있어야**
  하며, 그 값을 판정 이력에 함께 남긴다(`db.harness_runs`). 검사에 넣을 수 없는 파라미터는 검증된 적이 없다.
- **판정은 사전등록으로만 확정한다.** `harness_last.json` 1슬롯 덮어쓰기 + `cli.py`가 combos 루프 안에서
  저장하는 구조라 **8조합 스윕의 마지막 칸이 정본**이었다(우연 통과 확률 33.7%). 정본은
  `docs/preregistered.toml`에 조합·가설·표본 요건·판정 기준을 적고 **커밋한** 것만이며, 요건 충족일에 **1회**
  확정하고 동면한다. 탐색 실행은 이력에만 쌓이고 보드를 건드리지 않는다(`board_updated: false`).
  같은 가설을 두 번 보면(interim·final) 문턱을 올린다 — Šidák n=2 → 97.47%. **n은 파일 등록 수로 고정**한다:
  `status != locked`로 세면 interim이 확정되는 순간 final 문턱이 저절로 95%로 내려가는 사후 완화가 된다.
- **결과를 본 가설은 `from_date`로 아직 보지 않은 구간에만 걸어 등록한다(2026-08-06).** D4(추세
  게이트 off)는 진단 실행에서 백분위 92.5를 **보고 나서** 등록했다. 전 구간에 그대로 걸면 사후등록이라
  `requirement.from_date = 등록일+1`로 못 박았다 — 그 구간은 등록 시점에 존재하지 않았다.
  `prereg.load`가 `from_date >= registered_at`을 강제하고, 창을 자를 때 **점수·패널·커버리지를 같은
  위치에서** 자른다(`store._slice_after`) — 한쪽만 자르면 인덱스가 밀려 다른 날짜의 점수로 채점한다.
  그리고 **워밍업 과거를 먼저 잘라내지 않는다**(MA120·모멘텀 252일이 빠져 창 앞부분 팩터가 조용히
  사라진다): 점수 패널을 만든 **뒤에** 자른다.
- **설정이 다른 가설은 family를 나누고, 그래도 Šidák n은 줄지 않는다.** `prereg.load`가 같은 family
  안의 설정 불일치를 거부한다(순차 관측이 아니라 별개 실험이라 Šidák 가정이 깨진다). 대신 n은
  **파일 전체의 정본 look 수**로 세므로 D4를 더하며 문턱이 97.47 → **98.30%**로 모두 올라갔다.
  파일을 쪼개 n을 낮추는 것이 곧 사후 완화다.
- **반사실(counterfactual) family는 라이브와 다른 키를 선언해야 하고, 헤드라인이 될 수 없다.**
  `config_agrees_with_engine`이 D4를 거부하는 것은 옳다 — 라이브가 일부러 안 돌리는 설정이니까.
  그렇다고 검사를 통째로 끄면 **나머지 16개 키가 조용히 낡아** "라이브에서 한 가지만 바꾼 것"이라는
  전제가 거짓이 된다. `diff_from_live = ["trend_gate"]`로 선언한 키만 면제하고 나머지는 전부 대조한다.
  그리고 D4는 `role="final"`이지만 보드 헤드라인이 되면 안 된다 — 그 판정이
  `prereg.change_allowed`(N2)를 열면 **돌리지 않는 전략의 성적으로 돌리는 전략을 바꾸는 것**이다
  (`harness_board`가 `counterfactual`을 헤드라인 후보에서 뺀다).
- **요건 미충족 동안 백분위를 화면에 내지 않는다.** 매일 보이면 매일 보게 되고 그게 곧 다중검정이다.
  보드는 `판정 보류 · 실효 n/N · PIT n/N일`만 보여주고, 숫자는 이력 표에서만 본다(이력은 진단용, 보드는 판정용).
- **표본은 실효 기간으로 센다.** `periods`(전체 리밸런스 횟수)에는 후보가 비어 매수 0건인 기간이 섞인다 —
  실측으로 가격 경로가 `219기간 중 90이 매수 0건`이었다. `effective_periods = periods − empty`(최악 위상 기준)로
  세고, 미달은 경고가 아니라 **차단**이다. 그리고 **완화가 아무 것도 안 막는지 확인한다** — PIT에서
  `min_periods`를 5로 낮춘 코드는 효과가 없으면서 있는 것처럼 보였다.
- **시도 횟수를 세서 문턱에 반영한다(2026-08-06, L4→L3).** 사전등록 Šidák은 "같은 가설을 몇 번
  볼까"를 막지만 그 앞단의 "탐색으로 몇 개 조합을 돌려봤나"는 보정되지 않았다. `harness_runs`가
  append-only라 세면 된다 — `db.harness_trial_counts`가 **고유 조합 수**(재현은 새 시도가 아니다)와
  바꿀 수 있는 파라미터 수를 낸다. 그 N을 Deflated Sharpe(`multiplicity.deflated_sharpe`)가
  `E[max SR]`에 쓴다: N=1이면 0, N=8이면 0.19, N=1000이면 0.42로 문턱이 올라간다.
- **DSR을 붙일 때 두 번 틀렸고 둘 다 관대한 방향이었다.** 처음 붙였을 때 **DSR 0.979 "유의"** 가
  나왔는데 같은 실행의 백분위는 71.5(판정 불가)였다 — 두 판정이 갈라지면 **관대한 쪽이 읽힌다**.
  원인 ① 5위상을 이어 붙여 T=1093으로 썼다: 위상은 구간이 겹쳐 독립이 아닌데 `sqrt(T−1)`이
  33.0(독립 219 기준 14.8)으로 부풀었다. ② DSR은 `Sharpe > 0`을 검정하므로 **롱온리 전략이
  상승장에서 종목선택 능력 없이 통과**했다. **초과수익(전략−벤치마크) · 한 위상**으로 고치니
  0.979 → **0.282**가 됐고 백분위와 방향이 맞았다. 산출물에 `basis`·`phases_pooled`를 싣고,
  `_verdict`는 DSR을 보지 않는다(판정 경로는 하나여야 한다).
- **p-value는 단일 시드로 검증할 수 없다 — 오탐률로 잰다.** SPA(L2)를 만들고 시드 하나로 돌렸더니
  귀무인데 p=0.0012가 나와 코드 결함으로 의심했다. 150 시드로 재니 **오탐률 3.3%**(명목 5%)에
  강한 우위 검출률 92.7%였다 — 교정돼 있었고 그 시드가 드문 draw였다. 검정을 새로 만들면
  **귀무 오탐률과 대립 검출력을 같이** 재고 검사에 박는다(하나만 재면 "아무것도 통과 못 하는 검사"와
  "잘 만든 검사"를 구분할 수 없다).
- **홀드아웃 성적을 출력하면 그 구간은 더 이상 홀드아웃이 아니다(L1).** `--holdout-from`은 패널을
  **잘라낸다** — 문구로 "보지 않는다"고 적는 게 아니라 하류가 볼 수 없게 데이터를 없앤다.
  홀드아웃을 쓰려면 사전등록 `requirement.from_date`로 걸어 1회 확정한다.
- **SPA가 p를 낮춰도 "고른 것"이라는 사실은 남는다.** Hansen SPA는 조합 간 상관을 부트스트랩으로
  반영해 Šidák보다 덜 보수적이지만, 사전등록 없이 그 결과로 채택하면 여전히 사후선택이다.
  출력에 그 문장을 같이 쓴다.

- **측정되지 않은 것을 근거로 파라미터를 바꾸지 않는다.** 판별력이 `판정 불가`인 동안에는 가중치·
  부호·분위를 만지지 않는다. 한 구간에서 역방향이 98퍼센타일이 나와도 그건 곡선 맞추기다.
  하네스는 리포트가 아니라 **변경을 막는 게이트**로 쓴다.
- **게이트는 경로마다 세기를 다르게 둔다(2026-08-05).** 판정 전 파라미터 변경을 **전부** 잠그면
  진짜 바꿔야 할 때 `engine.py` 소스를 직접 편집하는 우회로가 생기고(H1이 그랬다) 그 변경은
  이력에 안 남는다. 반대로 전부 열면 게이트가 없는 것과 같다. 그래서 **자동 제안은 하드 차단**
  (`brain_proposals.refresh`·승인 — 큐를 비워 둔다. 쌓인 큐는 배지로 떠서 승인을 유도하는 압력이다),
  **수동은 사유를 받아 통과시키고 `unproven=true`로 이력·화면에 남긴다**
  (`prereg.change_allowed`). 미검증 변경을 막는 대신 **재무제표에 기록**한다.
  그리고 게이트가 읽을 수 없을 때는 **막는 쪽**이 안전하다 — fail-open은 게이트가 없는 것과 같다.
- **팩터를 백테스트에 넣을 수 없는 이유가 "데이터 없음"인지 "시점 정보 없음"인지 구분한다(2026-08-05).**
  가격 하네스가 3팩터였던 이유는 재무 이력이 없어서가 아니었다 — `fundamentals_history.json`에
  199종목 × 3년이 있었고 없던 것은 **"언제부터 알 수 있었나"** 였다(`signals/pit_fundamentals.py`,
  FY Y → (Y+1)-04-01). 그리고 그 6팩터 백테스트는 **백분위 53.5 = 무작위와 구분 불가**였다.
  3팩터일 때의 95.0은 모멘텀 단독 + 생존편향 유니버스의 산물이었다. **팩터를 빼고 잰 결과가
  좋으면 그건 좋은 신호가 아니라 무엇을 뺐는지 확인하라는 신호다.**
- **결과를 본 뒤에 사전등록하지 않는다.** 탐색으로 돌려 숫자를 본 조합을 그 뒤에 등록하면
  사후등록이고, 사전등록의 의미가 사라진다. 본 결과는 이력에만 남기고(`preregistered_id=null`),
  정본으로 삼으려면 **아직 보지 않은 구간**으로 요건을 걸어 새로 등록한다(OOS).
- **0에는 반드시 이유를 붙인다.** "아직 안 익어서 0"(정상)과 "수집이 끊겨서 0"(고장)이 화면에
  똑같이 보이면 고장을 몇 주씩 못 본다. 실제로 시세 캐시가 07-03에서 멈춘 채 시그널만 07-24까지
  쌓여 정확도 전 항목이 null이었는데 화면은 "누적 중"이었다(`accuracy.coverage.blocked_reason`).
- **정지는 파일 신선도로 안 잡힌다 — 거래일 달력과 대조한다(2026-08-05).** 시세가 백필로 최신이면
  `stale_prices=false`가 되어 **PIT 스냅샷 정지를 가린다**(`blocked_reason`도 null이었다). mtime을
  보는 어느 플래그도 이걸 못 잡는다. `store.pit_gap_days()`가 스냅샷 날짜를 `_market_dates()`와
  대조해 결측 거래일을 **이름으로** 낸다 — "7건"만 적으면 어느 날이 빈지 몰라 조사가 안 된다.
  그리고 정지 탐지는 **아침 브리핑 첫 줄**이다(`digest.stall_line`). 아래로 밀면 안 읽히고,
  멈춘 데이터로 아래 숫자를 오늘 값이라 믿는다. 정상일 때는 아무 말도 하지 않는다 —
  매일 초록불을 쓰면 그것도 곧 안 읽힌다. 화면 배너는 **서버가 만든 같은 문장**을 그대로 쓴다.
- **자동 루프에 넣을 때 임계는 한 곳에만 둔다.** 수동 버튼 전용이던 소스 5종(거시 FRED·ECOS·
  거장 13F·미국 실적일정·연도별 재무)을 일일 루프에 넣되 **매일 부르지 않고 `data_freshness()`가
  stale이라 할 때만** 부른다(`api._is_stale`). 주기를 루프에서 따로 정하면 화면이 말하는 신선도와
  실제 갱신 주기가 갈라진다. `data_freshness()` 목록에 없는 소스는 **낡아도 화면에 안 뜬다** —
  새 캐시 파일을 만들면 그 목록에 등록하는 것까지가 한 세트다.
- **수집 코드가 있다고 데이터가 갱신되는 건 아니다(2026-07-26).** 위 시세 정지의 원인은 수집
  실패가 아니라 **아무도 안 부른 것**이었다 — `fetch_prices` 호출처가 CLI와 관리자 버튼뿐이고
  일일 루프에 없었다. 새 데이터 소스를 붙이면 (a) 자동 루프에 넣었는지, (b) 그 루프가 다른
  기능(봇 활성화 등)에 딸려 있지 않은지, (c) 갱신이 멈추면 화면에 드러나는지까지가 한 세트다.
- **내부 ID와 제공자 심볼을 분리한다(2026-07-26).** 클래스주는 표기가 제공자마다 다르다 —
  S&P500 원본 `BRK.B`를 내부 ID `BRK-B`로 바꿔 저장하고 그 표기를 그대로 외부에 물어서 토스는
  404, KIS는 세 거래소 모두 실패했다. 내부 ID는 고정(관심종목·보유·parquet가 이걸 쓴다)하고,
  외부 호출은 **후보 표기를 순서대로 시도해 통한 것을 캐시**한다(`us.symbol_variants`).
  문서로 규약을 확정할 수 없을 때 하나를 골라 박지 말고 시도해서 알아내게 만든다.
- **영구 실패는 기억해서 유예한다.** 실패를 기억하지 않으면 어떤 표기로도 못 받는 종목을 30분마다
  영원히 재시도하며 로그를 채우고, 배치 앞자리를 차지해 받을 수 있는 종목까지 막는다. 연속 실패
  횟수를 남겨 자동 루프에서만 유예하고(`store.us_price_deferred`), 사람이 누른 수동 갱신은 유예를
  무시한다. 유예 목록은 하루 한 번 이름과 함께 로그로 드러낸다 — 조용히 빠진 종목도 조용한 0이다.
- **커버리지 플래그 대신 실제 커버리지를 잰다.** "백필 완료" 불리언은 목표 깊이를 나중에 올려도
  래치가 걸린 채 얕은 데이터로 영구히 남는다. 실제로 목표를 5년으로 올린 뒤에도 400일치에
  머물렀다. 채워졌는지는 **저장된 데이터를 재서** 판단한다(`store.prices_need_deep_backfill`).
- **검증은 기계가, 가설은 LLM이(2026-07-26).** 에이전트끼리 검토시키면 같은 사각지대를 공유하고
  동의하는 쪽으로 수렴한다 — "방법론이 타당해 보입니다"는 버그가 셋 있어도 나온다. LLM 감사
  (`audit.py`)는 **반증 가능한 가설만** 쓰고 판정·제안은 금지하며, 승격은 사람이 `test_redteam.py`
  로 옮길 때만 한다. 새 검증 아이디어는 에이전트가 아니라 **반증 가능한 검사**로 먼저 표현해본다.
- **검사를 조일 때마다 양성 대조군을 같이 돌린다.** 촘촘하게 만들다 보면 아무것도 통과 못 하게
  되기 쉽고, 그러면 `판정 불가`는 정보가 아니라 고장 신호다. 대조군 시장은 **그 팩터가 원래
  잘해야 하는 시장**이어야 한다 — RSI 기반 기술 팩터는 과매수를 피하므로 "꾸준히 오르는 시장"에서
  일부러 진다.
- **게이트가 죽었는지는 데이터 유무로 확인한다(2026-07-27).** `fetch_warnings` 호출처가 관리자
  수동 갱신 하나뿐이어서 `warnings.json`이 존재하지 않았고, `load_warned_tickers()`가 늘 빈 집합을
  돌려주며 **토스 투자경고·거래정지 매수 veto가 한 번도 발동하지 않았다.** 시세 정지와 같은 병이
  게이트에서 재발한 것이다. veto·게이트를 추가하면 (a) 일일 루프에 호출이 있는지, (b) 데이터가
  0일 때 '정상 0'인지 '미수집 0'인지 화면에서 구분되는지(`store.warnings_status`)까지 한 세트다.
- **같은 점수를 두 곳에서 조립하지 않는다(2026-07-27).** 봇의 국내 경로가 `evaluate`에 `shorts`를
  안 넘겨서 화면의 '매수 후보'와 봇이 실제 산 종목이 갈라져 있었고, 그 차이는 어느 화면에도
  나타나지 않았다. 입력은 한 함수(`store.kr_engine_inputs`)로 모으고, `evaluate` 시그니처와
  대조하는 검사를 둔다(`test_redteam.test_bot_and_ui_score_from_one_input_set`).
- **PIT 스냅샷은 채점과 같은 가격 기준으로 찍는다.** 장중 실시간가 오버레이가 남은 상태로
  스냅샷을 찍으면 장중가 점수가 저장되는데 `accuracy`는 종가로 채점한다 — 같은 날짜에 두 기준이
  섞이면 실측이 조용히 오염된다. 스냅샷 직전에 오버레이를 걷고 거래일은 KST로 쓴다.
- **실패한 조회는 낡은 값을 남기지 않는다.** 시세 조회가 죽었을 때 오버레이를 유지하면 고정된
  장중가가 계속 시그널·체결가로 쓰인다. '오래된 종가'는 정직한 상태지만 '고정된 장중가'는 거짓말이다.
- **임베딩 문턱은 백엔드마다 다시 잰다(2026-07-27).** 해시 폴백에 맞춰둔 절대 cosine 문턱(0.78)을
  실제 의미 벡터(e5-small)에 그대로 쓰면 전부 오탐이다 — 무관한 한국어 문장끼리도 0.8~0.9가 나오고,
  악재 프로토타입 대비 중립뉴스 0.891~0.916 / 실제 악재 0.900~0.957로 **겹친다.** 절대 문턱을 만들
  수 없을 때는 **중립 앵커 대비 마진**으로 바꾼다(`kb_embed.EVENT_SEMANTIC_MARGIN`). 검색도 같다 —
  dense는 '아무것도 안 맞음'을 못 가르므로 후보는 BM25가 정하고 dense는 재정렬만 한다. 그리고
  `semantic_capable()`이 False인 해시 폴백은 화면에 이유를 노출한다(벡터가 저장돼 있다는 사실은
  그게 의미 벡터라는 뜻이 아니다).
- **shadow에는 판정 규칙과 판정 날짜를 같이 넣는다.** 판정 조건이 없는 관측은 영원히 안 본다.
  통계는 한 구현을 공유하고(`accuracy.diff_verdict` — 표본 수가 아니라 유의성으로 판정),
  축적만 하는 데이터는 **언제 측정 가능해지는지**를 계산해 노출한다(`store.consensus_readiness`).
  단 '측정 가능'은 '반영'이 아니다 — 판별력 확인 전에는 점수에 넣지 않는다.
- **배치 루프는 항목별로 격리한다(2026-07-27).** `kb.refresh`의 종목 루프에 per-target 예외 처리가
  없어서 한 종목의 수집 실패가 **나머지 종목 다이제스트 + `db.kb_prune()` + 증분 임베드를 전부**
  건너뛰었다. 정리가 마지막 한 줄이었기 때문에 인사이트 원문이 보존 한도(60건)를 넘겨 쌓였다.
  루프 안의 실패는 (a) 항목별로 잡아 계속 진행하고, (b) **어느 항목이** 실패했는지 이름으로 남기고
  (`kv:kb_refresh_last`), (c) 정리·후처리는 실패와 무관하게 실행한다.
- **집계 신선도는 정지를 가린다.** KB 신선도를 전체 다이제스트의 `max(updated)`로 재고 있었는데,
  거시(`_MARKET`)·US 다이제스트가 매일 갱신돼서 **국내 종목 수집이 7일 멈춘 동안에도 화면은 '0.2시간
  전'** 이었다. 신선도는 **그 기능이 대상으로 삼는 집합**에서 재고, 밀린 항목 이름까지 낸다
  (`kb.refresh_status` — 대상 25종목 중 신선 0). 평균·최대는 커버리지의 반대말이다.
- **품질 게이트를 예측력으로 읽지 않는다(2026-07-27).** KB 적재 게이트가 보는 것은 출처 등록·활성·
  scope + 광고·스팸·무관·허위 차단이고 소스 점수도 `수락률 − 거절률`이다 — **형식적 신뢰도**다.
  "강한 기준을 통과했으니 믿을만한 인사이트"는 base rate 없는 비율과 같은 착각이다. 정성 정보의
  값어치는 **사후 채점**으로만 말할 수 있다(`signals/kb_coverage.py` — 원문 있는 매수 후보 vs 없는
  후보의 실현수익률, 판정은 `accuracy.diff_verdict`). 그리고 채점에 쓸 상태값(문서 수 등)은 **그날
  PIT로 남긴다** — 나중에 `fetched`로 재구성하면 prune이 지운 만큼 과소집계돼 비교가 편향된다.
- **정성 근거는 시점 없이 내보내지 않는다(2026-07-27).** 오전에 사실이던 시황은 한 시간 뒤 사실이
  아닐 수 있는데, 검색 코퍼스가 `published`·`fetched`를 아예 버려서 **3주 전 기사가 오늘 기사와 같은
  순위**로 올라왔다("실적 서프라이즈" 최상위가 27.5일 전 문서였다). 규칙: (a) 나이를 **유형별 반감기**로
  감쇠해 순위에 넣는다(시황 2일 · 뉴스 4일 · 실적·공시 30일 · 리포트 45일 — 정보의 성질이 근거다),
  (b) `age_days`·`as_of`·`stale`을 **모든 결과에 싣는다**(소비자가 시점을 모르면 신선도는 없는 것과
  같다), (c) 시점 불명을 최신으로 대우하지 않는다(중간값 0.5 — 날짜 없는 업로드가 상위를 먹는다),
  (d) 감쇠는 순위에만 쓰고 판정에는 쓰지 않으며 오래된 문서를 지우지도 않는다(맥락은 남는다).
- **설명과 결정이 다른 데이터를 쓰면 사후 합리화가 된다(2026-07-27).** 점수는 8팩터로 나오는데 챗봇은
  KB 원문을 검색해 답한다 — 그대로 두면 "이 기사 때문에 매수"라는 없는 인과가 만들어진다. 근거(점수를
  만든 입력)와 맥락(배경 자료)은 **도구 산출과 시스템 규칙 양쪽에** 라벨을 박는다(한쪽만이면 프롬프트를
  고칠 때 조용히 사라진다 — `test_redteam.test_context_docs_are_labeled_as_not_being_the_score_basis`).
- **min-max 정규화는 없는 차이를 최대로 벌린다(2026-07-27).** 후보 집합의 폭에 맞춰 늘리기 때문에
  **후보가 적으면 사소한 차이가 전부**가 된다. 제목 길이만 다른 동점 문서 2건이 1.0 대 0.0이 되어
  최하위가 `점수>0` 필터에 조용히 탈락했고(후보 2건이면 늘 1건만 반환), cosine 차이 0.002도 0.05 대
  1.0으로 벌어져 dense 가중 0.55를 그대로 먹었다. 척도가 **비율로 의미 있으면 max로 나누고**(BM25),
  **절대값이 무의미하면 평균 대비 고정 감도**로 환산한다(cosine — `DENSE_SPREAD`, 동점이면 중립 0.5).
  정규화 하한은 0으로 두지 않는다.
- **사람 확인 대기는 큐 길이와 만료 임박을 드러낸다(2026-07-27).** 후보 이벤트는 5일 뒤 자동 만료되니
  '오래된 악재로 매수를 막는' 사고는 없지만, 반대로 **아무도 안 보면 유효한 악재가 반영도 안 된 채
  사라진다.** 사람을 루프에 넣었으면 (a) 대기 건수와 (b) 만료 임박 건수를 화면 배지로 올린다
  (`db.kb_event_queue_status`). 만료는 되돌릴 수 없어서 사후에 알아채도 소용이 없다.
- **관리자 화면은 관심사로 묶고, 진단은 눌러야 보이지 않게 한다(2026-07-27).** 기능이 늘면서 엔진 탭
  하나에 헬스·제안·실측·shadow·감사·리텐션·LLM비용이 세로로 쌓여 있었고, 데이터 진단은 버튼을 눌러야
  나왔다(=아무도 안 본다). 5축으로 나눴다 — **운영·데이터 / 엔진 / 검증·실측 / KB·리서치 / 콘텐츠**.
  규칙: 진단은 탭 진입 시 자동 로드하고 이유 있는 0의 개수를 **서브탭 배지**로 알린다, 값을 바꾸는
  액션과 읽는 정보를 같은 카드에 두지 않는다, 수 분 걸리는 수집·백필은 접이식(`<details>`) 안에 둔다.
- **리셋할 수 있는 장부는 track record가 아니다(2026-07-27).** 개인 페이퍼 봇은 시드 변경·성향 변경·
  초기화가 전부 유저 손에 있었다 — 성적이 나쁘면 초기화하면 그만이라 남아 있는 장부는 항상 좋아 보인다.
  백테스트에서 그렇게 경계한 **생존편향과 같은 병**이고, 신뢰를 쌓으려던 기능이 신뢰를 갉아먹는 구조다.
  증거로 쓸 장부는 **리셋 불가·시드 고정·읽기 전용**이어야 한다(레퍼런스 3봇 공개 장부). 실사용도
  그 판단을 지지했다 — 개인 봇 5개는 전부 꺼져 있었고 체결이 0건이었다.
- **격리했다고 말하려면 uid가 있어야 한다.** `bot_decisions`에 소유자 컬럼이 없어서 모든 봇의 판단이
  한 저널에 섞였고, 그 저널을 (a) 전체 유저에게 보이는 신뢰 스트립 승률과 (b) LLM 매수 선별의 과거
  성패 맥락(`advisor.build_lessons`)이 읽었다. **유저 A의 연습 매매가 유저 B의 봇 매수에 스며드는
  경로**였다. "개인 계좌라 격리됨"은 계좌 테이블만의 얘기였고, 파생 지표는 격리되지 않았다.
- **중복 판단을 세면 정확도가 인기도로 바뀐다.** 성향이 다른 봇 3개가 같은 종목을 사면 같은 판단이
  3번 기록된다 — 셋 다 산 종목이 오르면 3승, 하나만 산 종목이 내리면 1패다. 실측 39건 중 고유
  (종목·날짜)는 31건이었고, 중복을 접자 승률이 22.2% → 33.3%로 뒤집혔다. 집계는 **판단 단위**로
  세고(`db._DECISION_DEDUP`), 몇 건이 접혔는지(`deduped_from`)를 같이 낸다 — 안 보이면 다시 생긴다.
- **기능의 대상 집합은 그 기능이 정한다.** 관심종목 시그널 변동 알림이 봇 루프 안에 있어서 대상이
  '봇을 켠 유저'였다 — 알림과 아무 상관 없는 조건이다. 개인 봇을 없앴다면 알림도 조용히 죽었을
  것이다. 대상 집합은 옆 기능의 on/off가 아니라 그 기능의 정의에서 뽑는다
  (`db.uids_with_ticker_favorites`).

- **파일 mtime 신선도가 한 달짜리 정지를 가렸다(2026-08-06, 재발).** `data_freshness`가
  `us_prices`를 **파일 mtime**으로 재서 "50시간 전"이라 했는데, 실제 종목별 마지막 봉은
  **중위 한 달 전**이고 503종목 **전부**가 갱신기 기준 stale이었다. 갱신기가 파일은 쓰면서
  (mtime 갱신) 봉은 못 늘리는 상태였다. *"정지는 파일 신선도로 안 잡힌다"* 는 규칙을 PIT 스냅샷에
  대해 적어 두고 **미국 시세에는 적용하지 않았다.** 이제 `_us_prices_freshness()` 가 마지막 봉
  날짜와 **뒤처진 종목 수**를 내고, 임계는 갱신기와 같은 `US_STALE_DAYS` 하나를 쓴다.
  그리고 `rows` 컬럼의 뜻이 소스마다 다르면(행 수 vs 종목 수) `note` 로 밝힌다.
- **동결을 고장으로 보고하면 고장 조사를 유도한다.** `kb.refresh_status` 가 `자동수집 OFF`(기본값,
  의도적 결정)인 상태를 `대상 44종목 전부 3일 이상 미갱신 — 수집 루프가 멈췄을 수 있다`로 보고했다.
  `auto_collect` 를 넘겨받아 **동결·고장을 다른 문장으로** 낸다. 0의 이유는 미완성(진입점 없음)·
  동결(의도적 정지)·고장(수집 끊김) 중 어느 것인지 말해야 한다 — 셋이 화면에 똑같이 0으로 보인다.

- **런타임에 읽는 파일이 배포 이미지에 있는지 검사한다(2026-08-06 프로덕션 점검).** #323~#337을
  전부 배포했는데 `/api/verdict`가 `판정 불가 · 사전등록 파일 없음`이었다. 코드가 아니라
  **Dockerfile**이 문제였다 — `COPY pyproject.toml README.md ./` + `COPY src ./src` 뿐이라
  `docs/preregistered.toml`이 이미지에 없었다. **로컬 테스트는 전부 통과한다**(파일이 있으니까).
  `test_runtime_read_files_outside_src_are_in_the_docker_image`가 `src/` 밖 상대경로 리터럴을
  전부 훑어 Dockerfile의 COPY와 대조한다(`data/` 밑은 볼륨이라 예외). 그리고 **이미지를 실제로
  빌드해 실행**해서 확인했다 — 검사만으로는 `.dockerignore` 상호작용을 못 본다.
- **`{"ok": False}`를 버리고 성공 로그를 찍으면 매일 실패하며 매일 성공처럼 보인다.** stale 자동
  갱신 루프가 `fn()` 반환값을 버렸다. `fetch_universe_history`가 `KRX_API_KEY 없음`으로 거부됐는데
  `자동 갱신(stale): PIT 유니버스`로 찍혔고, 파일은 안 생기는데 stale 판정은 계속 True라 **4일간
  같은 일이 반복되며 화면엔 아무 것도 안 떴다.** 반환값을 확인해 `kv:auto_refresh_last`에 이름과
  이유를 남기고 배너로 띄운다. 성공하면 그 항목을 지운다(오래된 실패가 유령으로 남지 않게).
- **프로덕션은 로컬과 다른 것이 깨진다 — 배포 산출물을 지문 감별한다.** 프로덕션 `index.html`의
  sha256을 로컬과 대조하는 것만으로 (a) 어느 커밋까지 배포됐는지, (b) 내가 머지했다고 믿은 것이
  실제로 들어갔는지를 동시에 알 수 있다. `/api/*`는 인증 미들웨어가 **라우팅 전에** 401을 내므로
  없는 경로도 401이다 — 라우트 존재 판독에 쓸 수 없다.

- **비용 상한은 라우트가 아니라 호출 모듈에 둔다(2026-08-06).** `/api/chat`에만 걸면 `llm`을 부르는
  나머지 10개 모듈(kb·audit·advisor·narrative·hypothesis·rebalance·bot·company·shortform·api)이
  조용히 우회한다. `llm.budget_state()`가 **모든** 호출자에게 걸리고, 라우트는 **폭주 속도**만 본다
  (`_chat_guard` — 상한은 총액, 레이트리밋은 한 사람이 한 번에 쏟는 양. 둘은 다른 것을 막는다).
  지출을 **못 읽으면 막는다** — `db.llm_spend_usd`가 0.0(안 씀)과 None(모름)을 구분하고,
  모를 때 통과시키면 fail-open이라 게이트가 없는 것과 같다.
- **"단일 호출 지점"이라는 전제는 확인해야 한다.** 예산 게이트를 `_post_json`에 걸었더니
  `stream_call`이 통째로 우회했다 — SSE는 자기 요청을 따로 만들고, 그게 하필 막아야 할
  `/api/chat/stream` 경로였다. `test_budget_gate_covers_every_network_call_site_in_llm`이
  `urlopen`을 부르는 함수마다 앞에 예산 판정이 있는지 검사한다.
- **차단을 None으로 돌려주면 "키 없음"과 같아 보인다.** 예산 초과는 `BudgetExceeded` 예외로 올린다 —
  `llm.py`의 `except Exception` 11곳이 전부 이걸 먼저 재발생시킨다. 안 그러면 화면에서
  "LLM 미연동"과 "예산 초과"를 가를 수 없다(0의 이유 규칙). 라우트는 429 + 이유 문장을 낸다.
- **저장소가 배포를 넘어 살아남는지 앱이 스스로 잡는다.** `data/cache/app.db`에 유저·레퍼런스 봇
  장부·PIT 스냅샷·판정 이력이 들어 있고, 볼륨이 없으면 **배포마다 전부 지워진다** — "리셋할 수
  있는 장부는 track record가 아니다"라고 적어 두고 리셋을 인프라가 대신 해 주는 상태다. 그리고
  지워진 것은 조용하다(새 DB가 만들어져 화면이 "누적 중"으로 보인다). 볼륨 설정을 코드가 알 방법은
  없으므로 **증상으로 판정한다**: `store.mark_boot()`의 부팅 카운터가 이전 프로세스를 기억하는지
  (`storage_report`). 배포 후 카운터가 늘면 볼륨이 있고, 1로 돌아오면 휘발성이다.
  정상일 때는 배너를 띄우지 않는다 — 매일 초록불은 곧 안 읽힌다.

- **라우트를 만들고 화면에 안 붙이면 그 기능은 존재하지만 닿을 수 없다(2026-08-06).** 라우트 112개
  중 **11개가 화면·CLI 어디서도 안 불렸다.** `product_reviewer`는 라우트 2개가 있는데 UI 버튼이
  없고 `kv:product_review_last`가 없어 **한 번도 실행된 적이 없었다**(`audit.py`와 역할도 겹쳤다) —
  제거했다. 더 중요한 건 `/api/pick-reason`인데, 이건 **북극성 A의 절반**("고른 이유를 사후
  재생")이고 화면이 없다 — 지울 게 아니라 붙일 것이다.
  `test_every_api_route_has_a_caller_or_a_stated_reason`이 새 고아 라우트를 잡고, 허용 목록은
  **이유를 하나씩 적게** 한다(통째로 스킵하면 새 고아가 조용히 섞인다). 목록 길이에 상한을 둬서
  자라기만 하는 것을 막고, 사라진 라우트가 유령으로 남는 것도 검사한다.
- **동결은 이미 내려진 결정이다 — 다시 건드리자는 제안을 하지 않는다.** X5의 원래 계획은 숏폼·D7·
  온보딩 삭제였는데 그 셋은 `docs/north-star-selection.md:49`가 **동결**로 적어 둔 것들이다.
  동결 ≠ 삭제이고, 지우는 건 V1(판별력)·V2(검증 속도)에 아무것도 더하지 않으면서 완성된 코드를
  없앤다. 그리고 **"0건"으로 삭제를 정당화하지 않는다** — 로컬 캐시가 0인 것과 프로덕션이 0인 것은
  다르다(PIT 결측을 최상위 발견으로 올렸다가 로컬 사본이라 강등한 것과 같은 함정).
  0을 봤으면 **왜 0인지**를 먼저 말한다: 미완성(진입점 없음)인지, 동결(의도적 정지)인지,
  고장(수집 끊김)인지.
- **화면 카피가 정체성과 어긋나면 그건 표면 문제가 아니라 틀린 것이다.** 헤더와 온보딩에
  "강의 실습용 (무료) 데스크"가 남아 있었다 — 이 리포는 `개인 R&D용 시스템 트레이딩 랩`이고
  외부 소비자 성장이 1순위가 아니다(CLAUDE.md 첫 줄). 옛 프레이밍의 카피는 기능을 지우기 전에 고친다.

- **PR을 만들기 전에 로컬 HEAD가 push됐는지 확인한다(2026-08-06에 놓쳤다).** 즉시조치 커밋을
  push하고 PR을 만든 뒤 모바일 수정을 **같은 브랜치에 커밋만** 했다. 스쿼시 머지는 PR이 아는
  범위(push된 것)만 가져가므로 모바일 수정이 조용히 빠졌고, 나는 "머지 완료"라고 보고했다.
  검증은 **배포된 산출물을 지문 감별**해서 잡았다(프로덕션 `index.html`의 sha256을 로컬과 대조 —
  `_SIG_NARROW_PX`·`q-pair`가 양쪽 다 없었다).

  확인은 **조상이 아니라 내용으로** 한다. `--squash`는 새 커밋을 만들므로
  `git merge-base --is-ancestor <내 커밋> origin/main` 은 **항상 실패한다**(고치는 중에 이것도
  틀렸다). 맞는 검사는 `git diff <내 커밋> origin/main -- <바꾼 파일들>` 이 비는지, 또는 특징
  문자열을 `git show origin/main:<파일> | grep` 하는 것이다.

  "PR을 올렸다"는 "머지됐다"가 아니고, "머지됐다"는 "내가 쓴 게 들어갔다"가 아니다.

- **판정 상태는 첫 화면 첫 줄이고, 백분위는 요건이 찬 뒤에만 쓴다(2026-08-06).** `판별력`이라는
  문자열이 `index.html`에 세 곳뿐이고 **전부 관리자**였다 — 시그널 첫 화면은 접힌 `<details>` 안
  백테스트 숫자를 대신 보여줬다. 이제 신뢰 스트립 첫 줄이 `판정 보류 · 실효 n/N · PIT n/N일`이고
  접힌 요약도 판정부터 쓴다(`verdictRow`). **보류 분기는 `percentile`을 읽지도 않는다** — 보드가
  실수로 실어 보내도 화면은 안 그린다(`test_first_screen_shows_the_verdict_before_the_score`).
- **첫 화면에 판정을 올리려고 관리자 라우트를 열지 않는다.** `/api/harness/preregistered`는 가설
  원문·설정 해시·이력이 붙은 관리자 전용이다. **판정 상태만** 내는 `/api/verdict`를 따로 뒀고,
  백분위는 `harness_board`가 이미 비운 값을 **그대로 쓴다** — 두 곳에서 조립하면 화면과 보드가
  갈라지고 그 차이는 어디에도 안 뜬다(`test_verdict_route_reuses_the_board_and_does_not_recompute`).

## 화면 규칙 (2026-07-27 리디자인에서 실제로 틀렸던 것)

- **`table-layout:fixed`에서 열을 없앨 때는 `<col>`을 `display:none` 한다 — 폭 0으로 두면 안 된다(2026-08-06).**
  모바일에서 `섹터` 열의 `th`/`td`만 `display:none` 하고 `<col>`의 **인라인 width는 그대로 둬서**
  보이지 않는 열이 30%(375px에서 103px)를 계속 예약했다 — **인라인 스타일은 미디어쿼리가 못 이긴다**.
  종목명이 `HD한국조선해`+`양`으로 쪼개지고 배지가 줄바꿈돼 행 높이가 80~119px로 들쭉날쭉했다.
  고치려고 `col` 폭을 0으로 줬더니 **더 나빠졌다** — fixed 레이아웃이 다음 열 몫을 0폭 열에 먹여
  배지 셀 폭이 **0**이 되고 `매수권밖`이 한 글자씩 세로로 쪼개졌다. 실측 대조:
  `width:0` → 배지 셀 0 / `display:none` → 148 ✓. 그리고 남는 열끼리 **100%를 다시 나눠 가져야**
  한다(안 하면 30%가 죽은 공간). 접는 폭(`_SIG_NARROW_PX`)은 CSS 미디어쿼리와 **같은 값**이어야
  하고 레드팀이 대조한다 — 다르면 열은 숨겼는데 폭은 남는다.
- **브레이크포인트를 넘나드는 리사이즈에 다시 계산한다.** 안 하면 세로로 열었다가 가로로 돌릴 때
  깨진 채 남는다. 리스너는 **하나만** 둔다(차트 resize 핸들러에 합쳤다) — 같은 일을 두 곳에서
  시키면 한쪽을 고칠 때 다른 쪽이 남는다.
- **라벨과 값은 한 덩어리로 묶는다.** `<span class="q-k">PBR</span> 1.57` 처럼 라벨만 span이고
  값이 맨 텍스트 노드면 flex-wrap이 그 사이에서 줄을 끊는다 — 375px에서 `PBR`과 `1.57`이 서로
  다른 줄에 놓였다. `.q-pair { white-space:nowrap }` 로 짝을 감싼다.
- **넓은 표는 자기 컨테이너 안에서 스크롤한다.** 조상 카드가 `overflow-x:auto` 라 "스크롤은 됐지만"
  카드 전체가 밀려 헤더·문단까지 따라 움직였다. `.tscroll > table` 로 표만 스크롤한다.

- **참조하는 CSS 변수는 반드시 `:root`에 정의한다.** `var(--sell)`이 48곳에서 쓰였는데 정의가 없어
  경고·매도 색이 전부 본문색으로 조용히 렌더됐다. 미정의 변수는 에러가 아니라 **무색**이라 아무도
  못 본다. 옛 이름(`--sell`·`--warn`·`--fg` 등)은 별칭으로 남기고, `test_smoke`가 `:root` 안에
  있는지 검사한다. 토큰 값을 바꿀 때는 **하드코딩된 옛 값도 같이 옮긴다** — 안 그러면 새 팔레트와
  구 팔레트가 한 화면에 섞인다(실제로 41곳이 구 값으로 남아 있었다).
- **`--x: var(--x)` 자기 참조는 토큰을 죽인다(2026-08-06).** `--sig-watch:var(--sig-watch)` 였다.
  CSS 순환 참조는 무효값이 되어 `getPropertyValue`가 **빈 문자열**을 돌려주고, 그걸 쓰는
  `--warn`·`--c-ma60`까지 무색으로 렌더됐다. 나는 이걸 모르고 **작동하던 `#b45309` 12곳을 이
  깨진 토큰으로 바꿔 회귀를 만들었다**(#341) — 하드코딩을 토큰으로 옮길 때는 **그 토큰이 실제로
  해석되는지 브라우저에서 확인**해야 한다(`getPropertyValue`가 빈 값이면 죽은 토큰이다).
- **팔레트가 "칙칙한" 것은 취향이 아니라 구조다.** 개편 전 수치: 유채색이 전부 극단 명도
  (브랜드·매수 L16~31%, 배경·라인 L81~95%)라 **중간 명도에 색이 없었고**, 브랜드(H174)와
  매수(H156)가 **18°**밖에 안 떨어져 매수 신호가 브랜드에 묻혔고, 배경은 웜 베이지(H40)인데
  브랜드는 쿨 청록(H174)으로 거의 보색이라 서로를 탁하게 만들었다. 색을 고칠 때는 **H·S·L을 재고**
  고친다. 검사도 값이 아니라 **규약**을 본다(브랜드-매수 hue 30° 이상, 브랜드 명도 28~55%) —
  hex를 리터럴로 못 박은 검사는 팔레트를 바꿀 때마다 깨지기만 한다(`test_smoke`가 그랬다).
- **토큰 값을 바꿀 때는 하드코딩된 옛 값도 같이 옮긴다.** 19곳이 옛 팔레트로 남아 있었고, 그중
  일부는 ECharts에 넘어가는 JS 리터럴이었다. 그리고 **ECharts는 `var()`를 해석하지 못한다** —
  차트 색에 `'var(--x)'` 문자열을 넘기면 조용히 기본색이 된다.

- **폴백 체인의 마지막까지 정의돼 있는지 확인한다(2026-08-06).** `var(--buy-strong,var(--brand))`
  가 **둘 다 `:root`에 없어서** 체인이 끝까지 비고 상속색으로 렌더됐다. 폴백이 있으면 안전해
  보이지만 폴백도 미정의면 똑같이 무색이다. 하필 위치가 앱에서 가장 중요한 상태(**확정 판정**)의
  색이었다. 검사는 **주석을 지운 뒤** 센다 — 설명 주석에 `var(--x)`를 적으면 오탐이 난다.
- **자동으로 펼쳐지는 카드 안의 진단은 접어 둔다.** `매수 0` 카드는 대부분의 날 자동 펼침이고,
  X3에서 게이트 사유를 늘리자 **439px**을 먹어 종목 표가 **827px**에서 시작했다 — 목록을 보러 온
  화면에서 목록이 접힌 셈이다(감사가 지적한 340px보다 나빠졌다. 내가 만든 회귀다).
  그리고 그 사유 5줄 중 둘은 카드 헤더가, 셋은 상단 시장바가 이미 말하는 것이었다.
  접은 뒤 카드 352px · 표 740px. **"0의 이유를 낸다"가 "장문을 항상 펼친다"는 아니다.**

- **10px 이하는 정보가 아니라 착시다.** 폰트 크기가 19종(10·11·11.5·12·12.5…) 흩어져 있었고 그중
  10~12px이 308회였다. 촘촘하면 정보가 많아 보이지만 읽히지는 않는다. **최소 11px, 8단계**
  (11·12·13·14·15·17·21·27)로 수렴시키고 `font-weight`는 600·700만 쓴다.
- **같은 말을 한 화면에서 두 번 하지 않는다.** 차트 설명이 상단 힌트·ECharts 내장 범례·하단 범례로
  3중이었고, 시각(`as_of`)은 패널 헤더와 범례에 중복이었다. 새 설명을 붙이기 전에 **그 정보가 이미
  화면에 있는지** 먼저 찾는다.
- **매일 보는 화면에 상설 용어사전을 두지 않는다.** 신뢰 스트립이 성적 1줄 + 페이퍼 1줄 + 용어 1줄로
  3층이었다. 처음 한 번만 필요한 설명은 접고(`.more-block`·`용어` 토글), 화면에는 숫자만 남긴다.
- **`hidden` 속성과 인라인 `display`를 같이 쓰지 않는다.** `<span hidden style="display:block">`은
  항상 보인다 — 인라인 스타일이 `[hidden]`을 이긴다. 접기는 `.more-block` / `.more-block[hidden]`
  처럼 규칙 한 곳에 묶는다.
- **같은 일을 두 곳에서 시키지 않는다.** 관심종목 등록이 리스트 ★과 히어로 CTA 버튼 두 군데였고,
  '매수만' 필터가 정밀도 스트립과 퀵필터에 각각 있었다. 진입점이 둘이면 사용자는 둘 다 안 쓴다 —
  경로를 하나로 접는다.
- **상시 노출 컨트롤은 3개까지.** 종목 패널 헤더가 6층(세그·정밀도·매수대기·검색·퀵필터·스크리너)
  이라 목록이 340px 아래에서 시작했다. 목록을 보러 온 화면에서 목록이 접힌 셈이다. 자주 안 쓰는
  필터는 스크리너 안으로 넣고, 조건부 필터(근접만)는 **해당 후보가 있는 날만** 띄운다.
- **부호가 아니라 크기로 색을 칠한다.** −0.02%를 빨갛게 칠하면 색이 '방향'이 아니라 '0보다 작음'만
  알려준다. 임계값(수익률 0.5%p·낙폭 1%p) 안쪽은 무채색으로 두고, 색은 실제로 볼 만한 변화에만
  쓴다. 배지도 마찬가지 — 한 행에 초록 태그가 3개면 정작 매수/매도 시그널이 안 보인다.
- **레이어 배지를 글자로 6번 반복하지 않는다.** 상세 탭 6개에 「신호」/「맥락」 배지를 달았더니
  배지가 탭 이름보다 먼저 읽혔다. 층위 구분(의견≠신호)은 유지하되 **점 하나로 낮추고 뜻은 범례에서
  한 번만** 말한다.
- **차트 팔레트를 기본값으로 두지 않는다.** ECharts 기본 색(보라·주황)은 이 서비스 어디에도 없는
  색이라 도넛 하나가 화면 전체 톤을 깼다. 차트 `color` 배열을 브랜드 톤으로 명시한다.
- **화면 변경은 실제로 렌더해서 확인한다.** 폰트·간격을 일괄로 올리면 좁은 폭에서 종목명이 3줄로
  쪼개진다. 데스크톱(1512)·모바일(390) 양쪽에서 **가로 넘침·콘솔 오류·접기 동작**을 브라우저로
  검사한다(`/tmp/sdshot/verify.mjs` 패턴).

## 미해결 / 확인 필요

- 시가총액 상위 200은 실제 코스피200 편입종목과 리밸런싱 시점 등으로 소폭 다를 수 있음(진짜
  편입종목 리스트는 공식 API에 없음 — BACKLOG.md §0 참고, 정확도가 중요해지면 data.krx.co.kr
  수동 다운로드로 교체 검토).
- 시장 국면 판정 룰(강세/과열/조정/약세) — 지수·금리·거래대금 임계값 설계 필요.
- 자동매매봇·전망 시나리오의 유사투자자문업 규제 해당 여부 — 법률 자문 필요(BACKLOG.md 하단 참고).
- **KIS 토큰 발급(oauth2/tokenP) rate limit 주의** — 짧은 간격으로 재요청하면 HTTP 403.
  `broker/kis.get_token()`을 거치지 않고 직접 발급 API를 호출하지 말 것(파일 캐시 우회 금지).
