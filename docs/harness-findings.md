# 포트폴리오 하네스 — 측정 결과

`sigdesk harness`로 "우리 점수 순위대로 사면 무작위로 사는 것보다 나은가"를 측정한다.

## 2차 측정 (2026-07-26, 5년 데이터)

이력을 400일 → **5년(1,221거래일)**으로 늘리고 대조군을 고친 뒤 다시 쟀다.

| 분위 | 보유 | 전략 누적 | 위상편차 | 대조군 중위 | 초과 | 백분위 | 판정 |
|---|---|---|---|---|---|---|---|
| 1% | 5일 | +144.5% | 308.8pp | +45.1% | +99.4pp | 91.7% | 판정 불가 |
| 1% | 20일 | +396.8% | 1021.8pp | +114.2% | +282.6pp | 100.0% | 판정 불가 |
| 3% | 5일 | +183.6% | 220.0pp | +68.3% | +115.3pp | 98.3% | 판정 불가 |
| 3% | 20일 | +320.0% | 628.0pp | +126.7% | +193.3pp | 100.0% | 판정 불가 |
| 5% | 5일 | +189.8% | 172.6pp | +82.8% | +107.0pp | 100.0% | 판정 불가 |
| 5% | 20일 | +316.3% | 471.7pp | +130.6% | +185.7pp | 100.0% | 판정 불가 |
| 10% | 5일 | +170.6% | 138.3pp | +93.5% | +77.1pp | 93.3% | 판정 불가 |
| 10% | 20일 | +281.3% | 202.7pp | +137.2% | +144.1pp | 100.0% | **판별력 있음** |

KR 200종목 · 1,221거래일(2021-07-27~2026-07-24) · 왕복비용 0.25% · 대조군 60시행.

**읽는 법: 아직 입증이 아니다.** 8개 조합을 한 번에 보면 판별력이 전혀 없어도 그중 하나가
95%를 넘을 확률이 약 34%다. 초록 한 칸을 골라 쓰면 그건 측정이 아니라 고르기다. 다만 1차
측정(전부 하위권, 초과수익 8개 중 7개가 음수)과는 달리 대부분이 91~100 백분위에 몰려 있다 —
"없다"에서 "있을 수도 있는데 아직 못 가른다"로 옮겨온 상태다.

막는 것은 여전히 **위상편차**다. 리밸런스 시작일을 하루씩 밀어보면 누적이 138~1,022pp씩
벌어진다. 가장 나쁜 위상까지 대조군 중위를 이겨야 `판별력 있음`인데, 좁은 분위(1·3%)일수록
편차가 커서 통과하지 못한다.

## 대조군을 고친 이유 — 회전율이 만든 가짜 판별력

셔플 검사(점수와 수익률의 짝을 어긋나게 하는, 정보량 0인 전략)를 5년 데이터에 돌렸더니
**백분위 100%로 `판별력 있음`이 나왔다.** 통과하면 안 되는 검사가 통과한 것이다.

원인은 대조군의 구조였다. 옛 대조군은 매 기간 k종목을 새로 뽑았고 회전율이 항상 100%라
거래비용을 최대로 물었다. 점수 기반 전략은 점수가 지속적이라 같은 종목을 계속 들고 있어
비용을 훨씬 덜 물었다. **정보가 하나도 없어도 비용 차이만으로 이긴다.**

| | 비용 0% | 비용 0.5% | 부의 배수 |
|---|---|---|---|
| 전략(점수 고정=회전율 0) | 0.980 | 0.852 | 0.869 |
| 옛 대조군(매 기간 재추출) | 0.999 | 0.503 | **0.503** |
| 새 대조군(라벨 치환) | 0.983 | 0.845 | 0.860 |

새 대조군은 **전략과 똑같은 시뮬레이터(`_run_phase`)에 라벨만 치환한 점수**를 넣어 돌린다.
점수의 시계열 구조를 그대로 물려받으므로 지속성·회전율·비용·게이트·보유 종목 수가 전략과 같은
분포로 유지되고, 남는 차이는 점수의 정보량뿐이다.

교훈은 대조군이 편향만 공유해선 부족하다는 것이다. **기계적 조건까지 같아야 한다.** 대조군을
별도 코드 경로로 짜면 그 경로의 차이가 판별력으로 둔갑한다.

### 이 버그가 검사를 빠져나간 경로

셔플 검사는 `tests/test_redteam.py`에 이미 있었는데도 못 잡았다. **`cost_pct=0.0`으로 돌리고
있었기 때문이다.** 비용이 0이면 회전율 차이가 결과에 나타나지 않는다. 검사와 현실 사이의 그
틈에 버그가 살았다. 지금은 비용을 켠 셔플 검사(`test_costs_alone_cannot_manufacture_an_edge`)와
회전율 정합성 검사(`test_the_null_pays_the_same_costs_as_the_strategy`)를 함께 돌린다.

## 커버리지 — '계산 불가'와 '조건 미발동'은 다르다

1차 측정에서 낙폭과대 커버리지 4.8%를 "이력 부족"으로 읽고 판정을 막았다. 그런데 낙폭과대는
**급락이 있을 때만 가중치를 갖는 조건부 팩터**다(엔진 설계대로다 — 평상시에도 가중치를 유지하면
매 종목 점수가 희석된다). 데이터를 5년으로 늘려도 발동률은 2.5%였다. 이걸 이력 부족으로 세면
데이터를 아무리 더 받아도 영원히 판정이 막힌다.

지금은 둘을 나눠 센다.

| 팩터 | 이력 커버리지 | 발동률 | 판정 차단 |
|---|---|---|---|
| 기술 | 100% | 100% | — |
| 낙폭과대 | 100% | 2.5% | 안 함(조건부 팩터) |
| 모멘텀 | 100% | 100% | — |

차단 사유는 **이력 부족뿐**이고, 발동률이 낮은 조건부 팩터는 "이 결과를 그 팩터의 성적으로 읽지
말 것"이라는 경고만 붙인다. 5년 데이터로 모멘텀은 5.9% → 100%가 됐다(252거래일 이력이 필요한데
캐시가 268일뿐이던 문제가 풀렸다).

재무·수급은 가격 재계산 하네스에는 넣지 않는다(룩어헤드). 대신 (1) PIT `signal_history`의
팩터 IC(`sigdesk accuracy`)로 실측하고, (2) `sigdesk harness --pit`이 스냅샷 점수 패널로
포트폴리오 판별력을 잰다(스냅샷 구간만, 보드 정본은 가격 하네스).

## 1차 측정 (2026-07-26, 400일 데이터)

400일치 캐시로 잰 첫 결과는 8개 조합 전부 `판정 불가`, 무작위 대비 초과수익은 8개 중 7개가
음수였다. 그 시점의 병목은 세 가지였고 지금 두 개가 해소됐다.

- **표본 부족** — 리밸런스 27회. 위상만 6거래일 밀어도 5일 보유 누적이 −4.7% → +61.3%로
  뒤집혔다. → 5년 확보로 217회. 여전히 위상편차가 크지만 결론을 완전히 삼키지는 않는다.
- **모멘텀이 조용히 빠짐** — 252거래일 이력이 필요한데 캐시가 268일이라 커버리지 5.9%.
  "8팩터 점수"의 실체가 사실상 기술 팩터 하나였다. → 해소.
- **PIT 스냅샷 부재** — 재무·수급 팩터가 백테스트 범위 밖. → 미해소.

## 하네스 자체에서 잡은 버그 4개

측정값을 믿기 전에 도구부터 의심해야 한다는 걸 네 번 확인했다.

1. **안 산 기간에 거래비용 차감** — 매수 0건이면 현금인데 왕복 0.25%를 물렸다. "모멘텀만"
   전략의 −13.9%는 수익률이 아니라 50기간치 수수료였다.
2. **동점을 유니버스 순서로 정렬** — 점수가 같으면 시총 순으로 뽑혀서, "점수가 없을 때
   대형주를 사는" 전략이 몰래 섞였다. 지금은 시드 셔플로 가른다.
3. **대조군만 단일 위상** — 전략은 위상 평균으로 분산을 줄이고 대조군은 안 줄이면, 전략이
   이긴 게 아니라 대조군만 흔들려서 이겨 보인다. 대조군도 같은 위상 평균을 거친다.
4. **대조군의 회전율이 전략과 다름** — 위 참고. 정보량 0인 전략에 백분위 100%를 줬다.

`tests/test_harness.py`의 양성 대조군(엣지가 실재하는 합성 시장)이 `판별력 있음`을 내는지 먼저
확인한다. 이게 깨지면 다른 모든 `판정 불가`는 발견이 아니라 도구 고장이다.

## H1 승격 (2026-08-03) — `weight_technical = 0`

사전 등록 ablation(trials=60, 3%/5일 고정, 스윕 없음):

| 실험 | p | 판정 |
|---|---|---|
| baseline (tech 0.35) | 95 | 판정 불가 (위상) |
| hold 20 | 100 | 판정 불가 (위상) |
| top 10% | 90 | 판정 불가 |
| **technical=0** | **100** | **판별력 있음** (최악 위상 > 중위) |

라이브 기본값·kv 옛 0.35 일회 제거: `SignalConfig.weight_technical=0`,
`signalcfg` H1 마이그레이션. 기술 팩터 **계산·게이트·reasons는 유지**, combine 가중만 0.
반증: 배포 후 harness(동일 3%/5일)가 다시 판정 불가·p≪95이면 롤백 검토.

### H1 재측정 (2026-08-04, 시세~08-04)

- 가격 하네스 3%/5일 · trials=60: **판별력 있음 · p95** (누적 +185%, 위상편차 175pp).
- PIT 매수 리프트(h20): 시그널 07-10~07-24라 아직 미성숙. h5 임시 헤드라인으로 IC 관측.
- 로컬 PIT 종류 분포가 거의 HOLD(BUY 1건) — 리프트 표본은 부족, **팩터 IC**가 단기 나침반.

## 그래서 무엇을 하지 않기로 했나

- **초록 한 칸(10%/20일)을 골라 쓰지 않는다.** 8개 중 하나가 우연히 통과할 확률이 34%다.
- ~~**가중치를 재조정하지 않는다.**~~ → H1만 예외(위). hold/top 동시 튜닝은 여전히 금지.
- **페이퍼 봇을 더 공격적으로 열지 않는다.** "거래를 늘리면 번다"는 근거는 아직 없다. 근거 없이
  회전율만 올리면 비용만 확정 손실로 쌓인다.

## 다음(우선순위)

1. **생존편향 제거** — 유니버스가 오늘 기준 상위 200이라 과거로 갈수록 부풀려진다. 대조군이
   같은 편향을 받으므로 백분위 판단은 성립하지만, 시점별 편입 종목(상장폐지 포함)을 쓰면 절대
   수익률도 의미를 갖는다. data.krx.co.kr 계정(무료)으로 pykrx 벌크 엔드포인트가 열리는지
   확인이 먼저다 — 지금은 로그인 요구로 시총 랭킹·상장종목 리스트가 전부 빈 응답이다.
2. **PIT 스냅샷 축적** — 재무·수급 팩터를 백테스트 범위 안으로 들여오는 유일한 경로.
3. **위상편차를 줄이는 규칙** — 좁은 분위일수록 달력 운에 좌우된다. 분위를 넓히거나 보유를
   늘리는 쪽이 안정적인데, 그 선택 자체를 백테스트로 고르면 과최적화다. 사전에 정하고 검증한다.
4. 1~3이 갖춰진 뒤에야 가중치·분위·보유기간 재조정. 그 전까지 하네스는 **변경을 막는 게이트**로
   쓴다: `판별력 있음`이 아니면 실매매 파라미터를 바꾸지 않는다.
